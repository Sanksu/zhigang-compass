# 前端响应式适配现状调研报告

> 调研范围：10 个核心页面 + 布局组件 + UI 基础组件 + 业务子组件
> 断点基准：Tailwind 默认断点（sm=640px / md=768px / lg=1024px）
> 评估维度：布局溢出、表格处理、表单栅格、断点完整性、字号间距、按钮挤压、Tabs 适配

---

## 一、已适配完善的页面（3 个）

### 1. 登录页 `src/routes/login-page.tsx`
- **布局**：居中卡片 `max-w-sm`，`px-4` 水平内边距，移动端天然友好
- **表单**：单列布局，无多列优化需求（登录表单天然单列）
- **字号**：标题 `text-xl`，描述 `text-sm`，移动端适中
- **结论**：适配完善，无需修改

### 2. 注册页 `src/routes/register-page.tsx`
- **布局**：与登录页同款居中卡片布局，`max-w-sm`
- **表单**：单列布局，三个表单字段垂直堆叠
- **结论**：适配完善，无需修改

### 3. 图谱页 `src/routes/graph-page.tsx`
- **布局栅格**：
  - 主布局 `grid-cols-1 lg:grid-cols-[1fr_320px]`，移动端单列
  - 详情面板 `GraphDetailRail` 实现了**移动端底部抽屉**（useSyncExternalStore 监听 1024px 断点）
- **Tabs 处理**：视图 Tabs 加了 `overflow-x-auto` + `whitespace-nowrap`，移动端可横向滚动
- **搜索栏**：`min-w-0 flex-1 sm:w-72 sm:flex-none`，移动端自适应宽度
- **操作按钮组**：`flex w-full flex-wrap sm:w-auto sm:flex-nowrap`，移动端可换行
- **图例区**：`lg:grid-cols-2 xl:grid-cols-4`，移动端单列堆叠
- **PageHeader**：`flex-col pb-4 sm:flex-row sm:items-center`
- **结论**：适配完善，是全项目响应式标杆

---

## 二、部分适配的页面（4 个）

### 4. 仪表盘 `src/routes/dashboard-page.tsx`
**已做的适配：**
- 指标卡 `grid-cols-2 lg:grid-cols-4` ✅
- 主内容区 `grid-cols-1 lg:grid-cols-3` ✅
- 数据源统计 `grid-cols-2 md:grid-cols-4 lg:grid-cols-7` ✅
- ECharts 图表有 `ResizeObserver` 自适应容器宽度 ✅

**缺口：**
- 最近活动时间轴：`min-w-0` 缺失，长文本可能撑破布局
- 快捷入口卡片：描述文字在窄屏可能偏长，但 `leading-relaxed` 可自动换行，问题不大
- **缺口等级**：低，主要是细节优化

### 5. 简历匹配页 `src/routes/resume-match-page.tsx`
**已做的适配：**
- 上传阶段 `grid-cols-1 lg:grid-cols-2` ✅
- 结果页主布局 `grid-cols-1 lg:grid-cols-[360px_1fr]` ✅
- 总分区 `grid-cols-1 md:grid-cols-[200px_1fr]` ✅
- 雷达+热力图 `grid-cols-1 md:grid-cols-2` ✅
- 差距分析：卡片化布局，非表格 ✅
- 学习路径：时间轴组件，非表格 ✅

**缺口：**
- 差距分析的证据溯源双栏（JD 要求 / 简历特征）`grid-cols-2` 在极窄屏（<375px）可能挤压，但有 `gap-2` 缓冲
- 推荐列表卡片在窄屏信息密度高，但整体可接受
- **缺口等级**：低，移动端基本可用

### 6. 新岗位发现页 `src/routes/discovery-page.tsx`
**已做的适配：**
- 指标卡 `grid-cols-1 sm:grid-cols-3` ✅
- 新岗位列表：卡片化（Card + 折叠展开），非表格 ✅
- 技能增减区：chip 化展示，`flex flex-wrap` 可换行 ✅
- 快照选择器：`flex flex-wrap items-center gap-2` ✅

**缺口：**
- Tab 切换用 Button 实现（非 Tabs 组件），`flex gap-2` 无 `overflow-x-auto`，窄屏可能溢出（但只有 2 个 Tab，问题不大）
- 岗位下拉 Select `w-72` 固定宽度，移动端可能超出视口
- **缺口等级**：低，仅细节优化

### 7. 管理仪表盘 `src/routes/admin-dashboard-page.tsx`
**已做的适配：**
- 指标卡 `grid-cols-2 md:grid-cols-4` ✅
- 数据源状态 `grid-cols-2 md:grid-cols-4 lg:grid-cols-7` ✅
- 两列布局 `grid-cols-1 lg:grid-cols-2` ✅
- 快捷操作 `grid-cols-2 md:grid-cols-4` ✅

**缺口：**
- 审计日志表格：4 列（时间/类型/操作人/详情），仅靠 `overflow-auto` 横向滚动兜底，无卡片化
- 详情列 `max-w-[200px] truncate` 截断，移动端信息损失
- **缺口等级**：中，表格类页面的通用问题

---

## 三、完全未适配的页面（3 个）—— 表格为主，仅横向滚动兜底

### 8. 数据血缘页 `src/routes/admin-lineage-page.tsx`
**具体问题：**
1. **表格列数过多（8 列）**：岗位名 / 证据 JD / 数据源 / 城市 / 置信度 / 技能印证 / 薪资 / 操作
   - 仅靠 `Table` 组件内置 `overflow-auto` 横向滚动
   - 无移动端卡片化降级方案
2. **统计卡**：`grid-cols-2 lg:grid-cols-5` —— 中间断点（md）缺失，平板端 2 列过宽
3. **过滤条**：`flex flex-wrap` 可换行 ✅，但搜索框 `w-56` 固定宽度在窄屏占比过大
4. **详情弹窗**：内部还有一个 7 列表格（证据 JD 血缘链），同样只有横向滚动
5. **组级校验摘要**：`grid-cols-2 sm:grid-cols-4` ✅（这部分没问题）

**优先级**：P1（管理端重要页）

### 9. LLM 决策日志页 `src/routes/admin-llm-decisions-page.tsx`
**具体问题：**
1. **表格列数过多（8 列）**：域 / 实体 / 状态 / 风险档 / 置信度 / Provider / 创建时间 / 操作
   - 仅 `overflow-auto` 横向滚动兜底
   - 操作列有「批准/驳回」两个按钮，移动端挤压严重
2. **统计卡**：`grid-cols-2 lg:grid-cols-4` ✅
3. **过滤条**：`flex flex-wrap items-center gap-2` ✅，但搜索框 `w-52` + 两个 Select `w-36` 加起来 320px+，窄屏会换多行
4. **审批弹窗**：自定义 `fixed inset-0` 实现，未用 Dialog 组件，但 `max-w-md` 在移动端可接受
5. **Tabs**：2 个 Tab（决策与验收 / 动态别名表），问题不大

**优先级**：P1（管理端重要页，有审批操作）

### 10. 审核工作台 `src/routes/admin-review-page.tsx` + 子组件
**页面级问题：**
- TabsList 无 `overflow-x-auto`，3 个 Tab（候选晋升审核 / 演化审核 / 岗位人工编辑）在窄屏可能溢出
- 子组件内大量表格，均无卡片化

**子组件具体问题：**

#### candidate-review-tab.tsx（候选晋升审核）
- 统计卡 `grid-cols-2 lg:grid-cols-4` ✅
- 布局 `grid-cols-1 lg:grid-cols-3` ✅
- 表格 5 列（岗位名 / 命中信号 / 发现时间 / 置信度 / 操作）—— 仅横向滚动
- 右侧说明看板在移动端移到下方（grid 单列）✅

#### evolution-review-tab.tsx（演化审核）
- 统计卡 `grid-cols-2 lg:grid-cols-3` ✅
- 演化审核表 5 列 —— 仅横向滚动
- 衰退归档表 4 列 —— 仅横向滚动

#### position-editor-tab.tsx（岗位人工编辑）
- 技能编辑表格 4 列（技能名 / necessity / weight / 删除）—— 仅横向滚动
- 文本定义编辑 `grid-cols-1 lg:grid-cols-2` ✅
- 移动端表格横向可滚，但编辑体验差（需要左右反复滑）

**优先级**：P0（核心管理页，审核是核心流程）

---

## 四、组件级评估

### 1. Tabs 组件 `src/components/ui/tabs.tsx`
- **现状**：`TabsList` 为 `inline-flex`，无 `overflow-x-auto` 兜底
- **问题**：Tab 数量多或文字长时，移动端会溢出父容器
- **各页面自行处理情况**：
  - 图谱页：加了 `overflow-x-auto` + `w-full justify-start` ✅
  - 审核页：未加 ❌
  - LLM 决策页：未加 ❌（但仅 2 个 Tab，问题不大）
- **建议**：在 TabsList 基础样式中加入 `overflow-x-auto`，或提供 `scrollable` variant

### 2. Table 组件 `src/components/ui/table.tsx`
- **现状**：外层 wrapper 有 `overflow-auto`，提供横向滚动兜底
- **问题**：
  - 仅有横向滚动兜底，无移动端卡片化方案
  - `TableHead` 的 `h-10 px-2` 在移动端可接受，但信息密度高
- **建议**：对于列数 ≤4 的表格，横向滚动可接受；列数 >5 的管理端表格需要卡片化降级方案

### 3. 审核子组件（5 个表格场景）
| 组件 | 列数 | 卡片化 | 优先级 |
|------|------|--------|--------|
| candidate-review-tab | 5 列 | ❌ 无 | P0 |
| evolution-review-tab（演化队列表） | 5 列 | ❌ 无 | P0 |
| evolution-review-tab（衰退归档表） | 4 列 | ❌ 无 | P1 |
| position-editor-tab（技能编辑表） | 4 列 | ❌ 无 | P1 |
| technology-watch-tab | 多列 | ❌ 无 | P2 |

### 4. 演化子组件
- **state-views.tsx**：六态分布 `grid-cols-3 sm:grid-cols-6` ✅，流转记录表格 ❌
- **watch-views.tsx**：技术热点观察池表格 ❌（多列：排名 / 技能 / 来源 / MLI 分 / 趋势）
- **结论**：演化页面的表格类视图均未做移动端适配

### 5. 顶栏 `src/components/layout/top-nav.tsx`
- **汉堡菜单**：`lg:hidden` 显示，✅
- **用户名/角色**：`hidden sm:inline`，小屏隐藏 ✅
- **品牌副标题**：`hidden sm:inline`，小屏隐藏 ✅
- **结论**：适配完善

### 6. 侧边栏 `src/components/layout/sidebar.tsx`
- **移动端**：抽屉式侧栏，遮罩层 + 滑动动画，`lg:hidden` ✅
- **桌面端**：固定 240px，`hidden lg:flex` ✅
- **结论**：适配完善

---

## 五、优先级排序

### P0 — 核心页面（影响核心用户流程）
1. **审核工作台**（`admin-review-page.tsx` + 3 个子组件）
   - 原因：审核是管理端核心流程，表格多、操作按钮多，移动端体验差
   - 缺口：3 个 Tab 溢出 + 5 个表格无卡片化 + 操作按钮挤压

### P1 — 重要页面（影响数据查看与管理决策）
2. **LLM 决策日志页**（`admin-llm-decisions-page.tsx`）
   - 原因：6 域决策验收是治理核心功能，8 列表格移动端无法使用
   - 缺口：8 列表格无卡片化 + 操作列按钮挤压
3. **数据血缘页**（`admin-lineage-page.tsx`）
   - 原因：跨源印证溯源是数据质量核心，8 列表格 + 详情弹窗表格
   - 缺口：8 列表格 + 弹窗内 7 列表格均无卡片化
4. **管理仪表盘**（`admin-dashboard-page.tsx`）
   - 原因：管理端首屏，审计日志表格移动端体验差
   - 缺口：审计日志表格无卡片化（程度较轻，4 列可接受横向滚动）

### P2 — 次要页面（信息展示为主，操作较少）
5. **新岗位发现页**（`discovery-page.tsx`）
   - 缺口：Select 固定宽度 + Tab 用 Button 实现无滚动（轻微）
6. **仪表盘**（`dashboard-page.tsx`）
   - 缺口：时间轴长文本潜在溢出（轻微）
7. **简历匹配页**（`resume-match-page.tsx`）
   - 缺口：差距溯源双栏极窄屏挤压（轻微）

### 已完善（无需处理）
- ✅ 登录页
- ✅ 注册页
- ✅ 图谱页
- ✅ 顶栏 / 侧边栏 / AppShell 布局框架

---

## 六、共性问题总结

| 问题类型 | 影响页面数 | 严重程度 |
|----------|-----------|----------|
| 表格仅横向滚动兜底，无移动端卡片化 | 6 个页面含表格 | 高 |
| TabsList 无 overflow-x-auto 基础能力 | 基础组件，波及所有用 Tabs 的页面 | 中 |
| 管理端页面列数普遍偏多（6-8 列） | 3 个管理页 | 高 |
| 操作按钮在窄屏挤压 | 所有带操作列的表格页 | 中 |
| 固定宽度的 Input/Select 在窄屏占比过大 | 4 个页面 | 低 |

---

## 七、建议改造方向（供后续迭代参考）

1. **表格卡片化**：列数 >5 的管理端表格，移动端改为「卡片列表」模式——每行数据转为一张卡片，关键字段突出显示，次要信息折叠
2. **Tabs 组件增强**：在 `TabsList` 基础样式中加入 `overflow-x-auto`，并提供 `scrollable` 或 `wrap` variant
3. **过滤条优化**：搜索框/Select 用 `flex-1` 替代固定宽度，窄屏自适应
4. **操作列简化**：移动端操作列只保留主操作按钮，次操作收入「更多」下拉
5. **断点补全**：部分 grid 布局缺少 `md` 中间断点，平板端体验可优化

# 源码打包校验报告（DO-M5-04 正式版）

> **打包日期**：2026-09-04（提交版，取代 09-03 版）
> **打包分支**：develop @ `3c5da641`（含 PR #787 赛题缺口补齐 + #788 用例矩阵勘误 + #789 打包脚本排除 LFS dump）
> **产物**：`zhigang-compass-3c5da641.tar.gz`

---

## 一、打包信息

| 项 | 值 |
|----|-----|
| 版本号 | `3c5da641`（git describe --always） |
| 压缩后大小 | 14M |
| 文件总数 | 1,238 |
| sha256 | `b10e298d043a7a42a43168c4607c2cc34719d529cbf30853bb46b757a95345c8` |

> ⚠️ 与 09-03 版（14M @ 7e18d318）相比：#786 引入的 `snapshots/*.dump`（Git LFS，共 173MB）曾被 `git archive` 以真实内容打进包（179M 事故）——#789 修正打包脚本将其排除。**数据快照作为独立数据交付物**随云盘提交（恢复：`scripts/restore_snapshot.sh`，见 snapshots/README.md）。

## 二、校验项（12 项全过）

**敏感文件排除（6/6 ✅）**：`.env`（backend/frontend）0、`.git/` 0、`llm_providers.yaml`（仅 `.yaml.example` 模板 ✅）、`*.pem` 0、`runtime_settings.json`（仅模板 ✅）、`skill_filters_dynamic.json`（仅模板 ✅）

**构建产物/大文件排除（6/6 ✅）**：`node_modules/` 0、`.venv/` 0、`__pycache__/` 0、`dist/` 0、`*.dump`（LFS 快照）0、媒体（mp4/pptx/wav/mp3）0

**必需文件完整性（12/12 ✅）**：DEPLOY.md、README.md、docker-compose.yml、backend/Dockerfile、openapi/openapi.yaml、黄金集 `final/jd_golden_110.jsonl`（110 条）、**测试数据交付物 `backend/data/test_artifacts/`（新岗位+既有岗位，#787）**、**`docs/design/测试用例矩阵.md` 索引（#788）**、设计文档 PDF、评测报告 PDF（docs/reviews/评测报告_初审版_20260903.pdf）、restore_snapshot.sh、snapshots/README.md

## 三、交付物对照（赛题 §五.2 / §八.二）

| 赛题要求 | 交付物 | 状态 |
|---------|--------|------|
| 设计实现方案 | `设计文档_初审提交版.pdf`（1.5M，docs/design/） | ✅ |
| PPT 作品介绍 | `智岗罗盘_答辩PPT_终稿.pptx`（2.0M，docs/m5/） | ✅ |
| ≤10min 演示视频 | `智岗罗盘_初审演示.mp4`（**8.8 分钟**，9.1M，docs/m5/） | ✅ |
| 完整源码 | `zhigang-compass-3c5da641.tar.gz`（本包） | ✅ |
| 部署说明 | `DEPLOY.md`（含 5 服务 compose + JWT fail-fast + 运维坑） | ✅ |
| 测试数据（1 新岗位 + 1 既有岗位含输入输出示例） | `backend/data/test_artifacts/test_data_{new,existing}_position.json`（#787） | ✅ |
| 黄金集 ≥100 条 | `jd_golden_110.jsonl`（110 条）+ 简历/匹配金标 | ✅ |
| 评测报告 | `docs/reviews/评测报告_初审版_20260903.pdf` | ✅ |
| 数据快照（独立附件，加分项） | `snapshots/pg.dump` + `snapshots/neo4j.dump` + `restore_snapshot.sh` | 独立交付 |

## 四、复现方式

```bash
git checkout 3c5da641 && bash scripts/package_release.sh
sha256sum zhigang-compass-3c5da641.tar.gz
# 期望: b10e298d043a7a42a43168c4607c2cc34719d529cbf30853bb46b757a95345c8
```

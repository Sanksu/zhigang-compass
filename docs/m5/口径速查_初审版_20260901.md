# 评审口径速查总表（初审版）

> **用途**：PPT / 演示视频 / 设计实现方案 / 评测报告取数的**单点归档**——所有对外宣称数字在此统一，任何一处变更须同步更新本表。避免「文档默认值 vs 实际运行值」与多处维护漂移。
> **数据截至**：2026-09-01 代码快照（develop 最新）。**09-04 打包前再次核对**。
> **红线**：不造假原则——双口径并列注明方法学；不得写"双人独立标注/Cohen's Kappa"（实为 A01 单标注 + QA）。

---

## 一、三项准确率（答辩主口径）

| 能力 | 指标 | 金标 | 评测脚本 | 达标(≥0.90) |
|---|---|---|---|---|
| JD 解析 | raw F1 **0.8865** / **aligned 0.9632**（词面豁免 PR #330+#488；08-28 正式验收三跑极差 0，precision 1.0000/幻觉 FP=0，见 `验收复测_20260828.md`；2026-09-02 校准，旧值 0.8731/0.9629 为 #331 合入前归档） | `jd_golden_110.jsonl`(110 条) | `scripts/evaluate.py --task jd` | ✅ aligned |
| 简历抽取 | **F1 0.988** | `golden_set_resume.jsonl`(50 条) | `scripts/evaluate.py --task resume` | ✅ |
| 人岗匹配 | **Acc 0.9714** / Spearman 0.7853 | `golden_set_match_v3.jsonl`(384 对) | `scripts/tune_match_weights_fourdim.py` | ✅ |
| RAG 接地检索 | **recall@5 1.0** / coverage 1.0（⚠️ recall@1 0.88 不作对外宣称） | 权威职业库 25 条（`backend/reports/rag_eval.json`） | `backend/scripts/archive/rag_eval.py`（已归档） | ✅ recall@5 |

> **人岗匹配口径演进**（避免引旧值）：v1 300 对 0.96（弃用）→ v2 384 对 0.8906（弃用）→ **v3 384 对 BT v4 0.9714（对外主口径，2026-09-01）**。education 进评分 w_edu=0.15，权重 0.669/0.044/0.287/0.15，判定线 0.57。

## 二、其他专项指标（辅助佐证）

| 能力 | 指标 | 说明 |
|---|---|---|
| 岗位职能域归类 | 严格通过率 **100%**(72/72) / pairwise F1 0.791 | `position_domain_eval.jsonl`(frozen2)，`scripts/evaluate_position_domains.py` |
| 学历匹配语义契约 | 82 条全部通过（2026-09-02 实测复跑，18 个对拍测试全绿） | `education_semantics_v1.jsonl`，对拍测试 |
| 学习路径 | 合理性 96.7%（专家定稿） | 学习路径评测报告，30 案例 |
| 技能分类 | 白名单 1093 词/20 类目；未分类 48.2%→8.3% | 图谱分类治理报告 |
| 岗位域命名覆盖 | 100%（123/123） | 域治理后 87 公开岗归 5 语义域 |
| 性能 | panorama P95 430ms / search 390ms @100 并发 | `perf_baseline_20260815.md` |
| 通货膨胀检测 | 召回率 ≥80% | 通胀检测报告 |

## 三、实现特质（评审亮点，可作答辩话术）

1. **职能域治理**：骨干域+归类制替代全员聚类——微簇域结构性消灭 + 逐岗可解释（domain_source/score 落图）+ LLM 成员自审
2. **匹配 BT v4**：education 进评分（w_edu=0.15），v3 金标含学历维度实证 Acc +5.7pp，向后兼容（v2 无学历字段逐位一致）
3. **多源可溯源**：每技能断言可追到原始 JD；匹配详情展示最佳匹配 JD 原文 + "同岗 N 条 JD 中最佳"
4. **幻觉防控三道防线**：Pydantic 强校验 / 双归一白名单 / 证据引用
5. **可回退设计**：全部算法/治理变更带单点回退（config 单值 / pin 单条），答辩"改一个值即可回退"

## 四、代码与规模（09-04 打包日刷新）

| 项 | 值 | 备注 |
|---|---|---|
| 代码规模 | =（09-04 重跑 `git ls-files`） | 旧 111k 行 |
| 提交/PR | =（09-04 重跑） | 旧 1214 commits/484 PR |
| 黄金集 | 110 条（题目硬性 ≥100 ✅） | `backend/data/golden_set/final/` |

---

## 五、口径变更记录

| 日期 | 变更 | 涉及文档 |
|------|------|---------|
| 08-24 | 物料清单口径速查首版 | `初审提交物料清单_20260824.md` |
| 09-01 | 匹配 0.8906→0.9714；域治理 +100% 基准 | 本表、设计文档、PPT大纲、评测报告、物料清单、进度跟踪 |
| 09-02 | RAG 指标口径冻结（recall@5 1.0，recall@1 0.88 不对外）；三道防线答辩口径对照注落设计文档 §6.3（防线 2 按 08-21 H5 裁决 B 话术）；PPT Spearman 0.88→0.7853 | 本表、设计文档 §6.3、PPT大纲 |
| 09-02 | JD 解析口径校准 raw 0.8731→0.8865 / aligned 0.9629→0.9632（08-28 正式验收三跑值，旧值为 #331 合入前归档）；学历语义契约 81→82（实测行数） | 本表、评测报告_初审版_20260901、PPT大纲 |

# 初审演示视频 · 拍摄脚本（DO-M5-02，≤10min）

> 依据 `docs/m5/PPT大纲.md` 按页录制；目标时长 ≤10min（建议 9~9.5min 留剪辑余量）。
> 录制人：文档岗（张怀伟）执行录屏 + 剪辑；技术岗提供预热与素材（截图/报告）。本脚本为录制蓝本，逐镜可微调。
> ⚠️ 红线（沿用 08-19/08-21 口径裁决）：跨源交代表述为「每日计算并展示一致性」，不宣称「单源不入图已拦截」；数据统一以打包前最新快照口径，页脚注明「数据截至 …快照」。

---

## 贯穿案例（全程复用）
- **简历**：`resume_sample`（技术栈：Python / 前端 React + Vue / SQL / Docker 的项目简历，提前在 226 或用例库准备一份用于演示）。
- **主线岗位**：「前端开发工程师」（或备选「数据工程师」），从**匹配 Top-N → 岗位详情 → 差距分析 → 学习路径**一镜到底，展示"技能级"闭环；末尾演化/发现页用同一技能族做呼应。

## 演示前置（录制前必做，避免冷启动翻车）
- 系统为最新 develop 构建的镜像（api/worker/postgres/redis/neo4j 全健康，226 局域网）。
- 预热：跑一次 panorama / 匹配 / 演化 / discovery 端点填充 Redis 缓存 + LLM provider 健康检查通过（见 `初审提交物料清单_20260824.md` §六 风险 4）。
- 用非敏感演示简历与数据；管理后台无真实口令泄露（admin 口令走 env 注入）。

---

## 分镜时间轴（9.5min）

| 时间 | 镜号 | 画面（屏幕位置/操作） | 旁白要点 |
|------|------|------------------------|----------|
| 0:00-0:20 | 1 | 封面卡（可从管理员首页顶部入镜） | 项目名 + 定位：多源异构驱动的岗位能力动态演化与人岗匹配系统 |
| 0:20-1:00 | 2 | 背景卡（静态/动画，非系统） | 技术迭代 vs 培养周期错配；招聘 JD 时滞/抄袭/通胀；缺技能级诊断 |
| 1:00-1:40 | 3 | 登录 → 仪表盘 | 整体总览：图谱节点/采集/版本/最近活动；五服务容器化架构一句话 |
| 1:40-2:30 | 4 | 爬取管理 `admin/crawl`（实时+历史）→「调度与限频」Tab | 13 源 A/B/C 分级、代理/合规、ETL 每日调度；切换 Tab 展示「调度与限频」配置 |
| 2:30-3:15 | 5 | 能力图谱 `/graph` ∨ 「大屏演示」，四视图（panorama→techStack→level→positionCenter），域聚合下钻 | 2D 力导向（≥100 节点）、动态加载 3D、岗位职能域聚合；大屏模式演示顺带说明 |
| 3:15-4:15 | 6 | 简历匹配 `/resume-match`：上传简历 → 匹配 Top-N 列表 → 进入岗位详情 | 三维评分（技能/经验/学历）+ 语义增强；Top-N 真实分数 |
| 4:15-5:00 | 7 | 岗位详情 → 差距分析卡（缺必备/熟练度） | 技能级差距诊断 + 优先级；证据链（JD 证据）可追溯 |
| 5:00-6:00 | 8 | 学习路径 `/resume-match` 路径卡（时间轴/先修链/课程） | 先修链展开、课程匹配（LEARNABLE_VIA + 语义）、学时甘特 |
| 6:00-7:00 | 9 | 演化看板 `/evolution` 3 Tab：「信号」（新兴/衰退 Top10）→「趋势与流向」（频次趋势/桑基）→「版本与状态机」（快照 diff/状态机/谱系） | 动态演化：Z-score 信号 + 快照追踪；告警与指标跨 Tab 置顶 |
| 7:00-7:40 | 10 | 新岗位发现 `/discovery`（近期新岗位 + 技能增减 diff） | 自动发现候选 + 旧岗位技能增减 |
| 7:40-8:30 | 11 | 管理后台：LLM 决策与验收 →「动态别名表」Tab + 字典守卫 `/admin/review/dict` | 幻觉防控三道防线 + LLM 决策透明可回放 + 自治守卫 |
| 8:30-9:00 | 12 | 性能/评测页（压测报告 + 评测数据卡） | P95 430/390ms @100 并发；110 条黄金集 aligned F1 0.9629；匹配 v2 Acc 0.8906 |
| 9:00-9:30 | 13 | 结尾卡 | 总结全链路闭环 + 展望（新岗位自动发现/领域扩展）+ 致谢 |

> 剪辑缓冲：每镜留 2~3s 首尾；总时长含片头/片尾标题，控制在 ≤10min。

---

## 技术规格
- 分辨率 **1080p**（1920×1080），H.264，建议 30fps；音频 44.1kHz 双声道中文解说。
- 输出 `智岗罗盘_初审演示_20260904.mp4`；保留工程文件便于返工。
- 片头/片尾标题与 PPT 一致（深蓝主色 + 力导向图元素）。

## 素材来源（技术岗在录制前提供）
- 架构图：`设计文档.md` §架构；图谱截图：前端 `/graph` panorama（4K 截图备裁）。
- 压测报告：`docs/perf_baseline_20260815.md`；评测：`docs/reviews/` 各专项 + `backend/reports/`。

## 录制后核对
- [ ] 总时长 ≤10:00；贯穿案例全程可见（简历→匹配→差距→路径）
- [ ] 无真实敏感数据（口令/简历 PII/API key）入镜
- [ ] 口径符合红线（跨源一致性表述、双口径并列数据、页脚数据截至快照）
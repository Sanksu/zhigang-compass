# 初审演示视频 · 拍摄脚本（DO-M5-02，≤10min）

> 依据 `docs/m5/PPT大纲.md` 按页录制；目标时长 ≤10min（建议 9~9.5min 留剪辑余量）。
> 录制人：文档岗（张怀伟）执行录屏 + 剪辑；技术岗提供预热与素材（截图/报告）。本脚本为录制蓝本，逐镜可微调。
> ⚠️ 红线（沿用 08-19/08-21 口径裁决）：跨源交代表述为「每日计算并展示一致性」，不宣称「单源不入图已拦截」；数据统一以打包前最新快照口径，页脚注明「数据截至 …快照」。
> 🔄 **09-01 口径同步**：本脚本已同步至 develop 最新——桑基图重构（#685）、岗位职能域治理·骨干域+归类制（#705~#723）、治理闭环（#686~#697，原始数据四类 raw 审计）、评测复跑（08-30，`backend/reports/eval_20260830.json`）、前端提速（#725）。08-27 旧稿的「四视图（含 level）」「匹配 Acc 0.8906」「简历 0.988」等口径已废弃，见下表。

---

## ✅ 录制前核对：与 PPT 终稿的数字一致性（09-01 已对齐）

PPT 终稿已按最新口径重出（`build_final.js` 09-01 版 + 重新导出页图 + 重新合成视频），口播稿与 PPT 数字已逐位一致：

| 指标 | PPT 终稿现值 | 口径出处 |
|------|------------|----------|
| JD 解析 aligned F1 | 0.9632（三跑一致） | `gold_acceptance_20260828.md` |
| 简历解析 F1 | 0.987（零幻觉误报） | `eval_20260830.json` |
| 人岗匹配 Acc | 0.9141（384 对黄金集 · Top-3 100%） | `eval_20260830.json`（#668 判定线 0.57 口径） |
| 岗位职能域 | 5 大骨干职能域 + 通用域兜底（归类依据逐岗可查） | `scripts/sync_position_domains.py`（08-31 治理） |
| 图谱视图 | 三视图（全景/技术栈/岗位画像） | 08-29 视图收敛 |
| 学习路径合理性 / P95 | 96.7% / 430·390ms（不变） | 专家定稿 / `perf_baseline_20260815.md` |

另：词面基线 F1 0.7622 为辅助口径（已知双口径），**不作为主指标宣称**；域治理基准 100%（72/72）为「与 AI 断言自洽」，基准集转正前不在视频中宣称独立精度。若后续评测数字再更新，须同步改 `build_final.js` → 重出 PPT → 重导页图 → 重跑 `generate_video.py`，四处一处不可漏。

## 贯穿案例（全程复用）
- **简历**：`resume_sample`（技术栈：Python / 前端 React + Vue / SQL / Docker 的项目简历，提前在 226 或用例库准备一份用于演示）。
- **主线岗位**：「前端开发工程师」（或备选「数据工程师」），从**匹配 Top-N → 岗位详情 → 差距分析 → 学习路径**一镜到底，展示"技能级"闭环；末尾演化/发现页用同一技能族做呼应。

## 演示前置（录制前必做，避免冷启动翻车）
- 系统为最新 develop 构建的镜像（api/worker/postgres/redis/neo4j 全健康，226 局域网；含 #685 桑基重构 / #705~#723 域治理 / #725 前端提速，**pull 后核对镜像 Created，防 GHCR 旧缓存**）。
- 预热：跑一次 panorama / 匹配 / 演化 / discovery 端点填充 Redis 缓存 + LLM provider 健康检查通过（见 `初审提交物料清单_20260824.md` §六 风险 4）；**匹配页首次冷启动含 SBERT 加载与匹配池化重建（约 60s），正式录制前先完整跑一遍匹配**。
- 前端已合入 GET 去重缓存 + 打包分包（#725）与整页入场过渡（#722），页面切换明显更顺滑，录屏时无需刻意等待加载。
- 用非敏感演示简历与数据；管理后台无真实口令泄露（admin 口令走 env 注入）。

---

## 分镜时间轴（9.5min）

| 时间 | 镜号 | 画面（屏幕位置/操作） | 旁白要点 |
|------|------|------------------------|----------|
| 0:00-0:20 | 1 | 封面卡（可从管理员首页顶部入镜） | 项目名 + 定位：多源异构驱动的岗位能力动态演化与人岗匹配系统 |
| 0:20-1:00 | 2 | 背景卡（静态/动画，非系统） | 技术迭代 vs 培养周期错配；招聘 JD 时滞/抄袭/通胀；缺技能级诊断 |
| 1:00-1:40 | 3 | 登录 → 仪表盘 | 整体总览：图谱节点/采集/版本/最近活动；五服务容器化架构一句话 |
| 1:40-2:30 | 4 | 爬取管理 `admin/crawl`（实时与历史）→「调度与限频」Tab | 13 源 A/B/C 分级、代理/合规、ETL 每日调度；切换 Tab 展示「调度与限频」配置 |
| 2:30-3:15 | 5 | 能力图谱 `/graph`：三视图切换（全景→技术栈→岗位画像）+ 全景域聚合双击下钻（域→岗位→技能）→ 演示视角书签 + 大屏演示模式 | 2D 力导向（≥100 节点）、全岗位按职能域聚合常驻（骨干域+归类制）；3D 动态加载、WebGL2 不可用自动降级 2D |
| 3:15-4:15 | 6 | 简历匹配 `/resume-match`：上传简历 → 匹配 Top-N 列表 → 进入岗位详情 | 四维评分（技能/经验/学历）+ 语义增强；推荐列表与详情同源 JD 级评分（同分可对账） |
| 4:15-5:00 | 7 | 岗位详情 → 差距分析卡（缺必备/熟练度 + 需求/趋势/ROI 三指标）→「JD 原文」展开溯源 | 技能级差距诊断 + ROI 优先级；证据链可回指原始 JD |
| 5:00-6:00 | 8 | 学习路径 `/resume-match` 路径卡（时间轴/先修链/课程） | 先修链展开、课程匹配（LEARNABLE_VIA + 语义 + 质量门控）、学时甘特 |
| 6:00-7:00 | 9 | 演化看板 `/evolution` 3 Tab：「信号」（新兴/衰退 Top10，Z-score 占比口径）→「趋势与流向」（频次趋势 + 岗位变迁桑基 + 技术观察池）→「版本与状态机」（多快照对比/状态机/谱系） | 动态演化：Z-score 信号 + 桑基流向（状态三色：持续在榜/新进榜/末次在榜，期次日期轴）；告警与指标跨 Tab 置顶 |
| 7:00-7:40 | 10 | 新岗位发现 `/discovery`（近期新岗位 + 技能增减 diff） | 自动发现候选（技术观察池 arXiv/GitHub 信号升级）+ 旧岗位技能增减 |
| 7:40-8:30 | 11 | 管理后台：LLM 决策与验收（「决策与验收」→「动态别名表」Tab）→ 字典守卫 `/admin/review/dict` → 原始数据 `/admin/raw`（JD Tab 快闪） | 幻觉防控三道防线 + LLM 决策透明可回放 + 自治守卫 + 四类 raw 全链路审计 |
| 8:30-9:00 | 12 | 性能/评测页（压测报告 + 评测数据卡） | P95 430/390ms @100 并发；110 条黄金集 aligned F1 0.9629；匹配 BT v4 Acc 0.9714 |
| 9:00-9:30 | 13 | 结尾卡 | 总结全链路闭环 + 展望（新岗位自动发现/领域扩展）+ 致谢 |

> 剪辑缓冲：每镜留 2~3s 首尾；总时长含片头/片尾标题，控制在 ≤10min。

---

## 技术规格
- 分辨率 **1080p**（1920×1080），H.264，建议 30fps；音频 44.1kHz 双声道中文解说。
- 输出 `智岗罗盘_初审演示_20260904.mp4`；保留工程文件便于返工。
- 片头/片尾标题与 PPT 一致（深蓝主色 + 力导向图元素）。

## 素材来源（技术岗在录制前提供）
- 架构图：`设计文档.md` §架构；图谱截图：前端 `/graph` panorama（4K 截图备裁）。
- 压测报告：`docs/perf_baseline_20260815.md`；评测：`backend/reports/eval_20260830.json`（08-30 复跑）+ `backend/reports/gold_acceptance_20260828.md`（JD 三跑验收）+ `docs/reviews/评测报告_初审版_20260829.md`。

## 录制后核对
- [ ] 总时长 ≤10:00；贯穿案例全程可见（简历→匹配→差距→路径）
- [ ] 无真实敏感数据（口令/简历 PII/API key）入镜
- [ ] 口径符合红线（跨源一致性表述、双口径并列数据、页脚数据截至快照）
- [ ] **口播数字与画面 PPT/页面数字逐位一致**（见上文「与 PPT 终稿的数字一致性」表，不允许新旧口径混排）

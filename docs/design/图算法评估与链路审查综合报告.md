# 智岗罗盘 · 图算法评估与链路审查综合报告

> 项目编号：XH-202621 · 科大讯飞挑战杯揭榜挂帅
> 本报告整合三份独立评估/审查文档（2026-08-12 ~ 08-13），保留各报告完整结论与数据：
> - **第一部分**：图算法参数评估报告（08-12，阶段一 γ 调优 + 阶段二 Leiden 验收 + 08-13 参数回退）
> - **第二部分**：数据采集链路 + 新岗位发现/图谱链路审查（08-13，P0-P2 断点与修复）
> - **第三部分**：JD 抽取 RAG 增强验证（08-13，盲审集 4 轮对比，title +19.4pp）

---

## 目录

- [第一部分：图算法参数评估报告（2026-08-12）](#第一部分图算法参数评估报告2026-08-12)
  - [1. 数据条件](#1-数据条件)
  - [2. 搜索空间与目标函数](#2-搜索空间与目标函数)
  - [3. 对比实验表](#3-对比实验表)
  - [4. 参数影响权重表](#4-参数影响权重表首轮扫描的经验发现)
  - [5. 最优配置结论](#5-最优配置结论)
  - [6. 后续建议（阶段二触发条件评估）](#6-后续建议阶段二触发条件评估)
  - [7. 阶段二验收：Leiden vs Louvain](#7-阶段二验收leiden-vs-louvain2026-08-12-实跑)
  - [8. 参数状态更新（2026-08-13 ETL 首跑后）](#8-参数状态更新2026-08-13-etl-首跑后)
- [第二部分：数据采集链路 + 新岗位发现/图谱链路审查（2026-08-13）](#第二部分数据采集链路--新岗位发现图谱链路审查2026-08-13)
  - [一、链路健康度总览](#一链路健康度总览)
  - [二、P0 断点（已修复）](#二p0-断点已修复)
  - [三、P1 断点（应修复）](#三p1-断点应修复)
  - [四、P2 断点（记录观察）](#四p2-断点记录观察)
  - [五、修复后验证](#五修复后验证2026-08-13-全量-etl-首跑)
  - [六、结论](#六结论)
- [第三部分：JD 抽取 RAG 增强验证（2026-08-13）](#第三部分jd-抽取-rag-增强验证2026-08-13)
  - [结论（TL;DR）](#结论tldr)
  - [实验历程（4 轮迭代）](#实验历程4-轮迭代)
  - [关键发现](#关键发现)
  - [逐条修正案例](#逐条修正案例第-4-轮)
  - [合入建议（产品化方案）](#合入建议产品化方案)
  - [遗留风险](#遗留风险)

---

# 图算法参数评估报告（2026-08-12）

> 项目编号：XH-202621 · 智岗罗盘
> 对应方案：图算法优化方案阶段一（γ 引入 + Optuna 参数扫描，跟踪入口：temp/剩余功能整合清单.md §1.1）
> 复现命令：
> ```bash
> cd backend
> uv run python scripts/graph_algo_tune.py --export data/graph_cooccurrence.json   # 导出共现图快照
> uv run python scripts/graph_algo_tune.py --snapshot data/graph_cooccurrence.json --trials 50 --apply  # 扫描并写回
> ```

---

## 1. 数据条件

| 项 | 值 |
|----|----|
| 快照来源 | Neo4j 技能共现网络（`load_skill_cooccurrence`，min_weight=1.0 全量导出） |
| 快照文件 | `backend/data/graph_cooccurrence.json`（2026-08-12 导出） |
| 节点数 | 386 技能 |
| 边数 | 3901 条（无向） |
| 权重口径 | 必要性组合因子 × 共现岗位数（must-must=1.0 / must-nice=0.5 / nice-nice=0.2） |

## 2. 搜索空间与目标函数

| 参数 | 范围 | 默认 |
|------|------|------|
| resolution γ | 0.5 ~ 2.0 | 1.0 |
| min_weight | 1.0 ~ 3.0 | 2.0 |

objective = **0.5·Q + 0.3·同质性 + 0.2·(1−过小簇占比)**（最大化），50 trial（TPE，seed=42）。

> **Q 评分口径说明**：Q 统一用**标准模块度（γ=1.0）**评估——γ 只负责生成划分，不参与评分。原因见 §4（首轮扫描发现 γ<1 时分辨率化 Q 虚高引导退化解）。

## 3. 对比实验表

| 配置 | γ | min_weight | Q（标准） | 同质性 | 过小簇占比 | 簇数 | objective |
|------|-----|-----------|-----------|--------|-----------|------|-----------|
| **默认**（调优前） | 1.0 | 2.0 | 0.1563 | 0.3673 | 0.1250 | 8 | 0.3662 |
| **Optuna 最优** | 1.256 | 2.502 | **0.1980** | **0.5588** | **0.0000** | 5 | **0.4666** |
| 变化 | +26% | +25% | +27% | +52% | −100% | −3 | +27% |

- 稳定性：最优参数 10 次独立重跑 objective std = 0.0000（Louvain 确定性算法，无随机性）
- 最优解非退化（5 簇、最大簇占比 0.3 < 0.5 阈值）

## 4. 参数影响权重表（首轮扫描的经验发现）

| 参数 | 影响方向 | 说明 |
|------|---------|------|
| γ 增大（>1） | 簇更细、同质性↑、Q↑ | 1.256 附近为平衡点；γ > 1.6 后过小簇开始增多 |
| γ 减小（<1） | 簇更粗 | **≤ 0.6 时退化为全部合并**（见下） |
| min_weight 增大 | 弱边过滤更多、簇更干净 | 2.5 附近过滤 must-nice 低频与 nice-nice 弱边更彻底 |
| min_weight > 2.8 | 边过少、节点孤立 | 过小簇/孤立簇开始反弹 |

**⚠️ 首轮扫描暴露的 objective 缺陷（已修复）**：γ<1 时"全部合并成单簇"是分辨率化 Q 的退化最优——单簇划分 Q=1−γ 虚高（γ=0.5 时 Q≈0.5）、同质性恒 1.0、过小簇占比 0，三项指标全部被单簇解主导，Optuna 直接收敛到 γ=0.501 的 1 簇解（objective 0.7493，无聚类价值）。

**修复**（`scripts/graph_algo_tune.py`）：
1. **γ 不参与评分**：objective 的 Q 统一用标准模块度（γ=1.0）计算
2. **退化解罚零**：簇数 ≤ 2 或最大簇占比 > 0.5 的划分 objective 直接 = 0

修复后重扫 50 trial 得到 §3 的最优配置，单簇解被完全排除。

## 5. 最优配置结论

```yaml
# backend/configs/graph_algo.yaml（已写入，API 默认参数随配置生效）
algorithm: louvain
resolution: 1.2564436070615108   # γ > 1：细簇方向，同质性显著提升
min_weight: 2.5021449703321417   # 弱边过滤更彻底（过滤 must-nice 低频与全部 nice-nice）
min_size: 2
```

- **结论**：γ=1.25 附近 + min_weight≈2.5 为当前图谱数据的最优平衡点，同质性 +52%、过小簇归零、Q +27%，技能簇语义更聚焦
- **API 接线**：`GET /graph/algorithms/skill-clusters?resolution=` 默认值随配置生效；缓存键含 resolution（γ 变更不串缓存）
- **回滚**：改回 `resolution: 1.0` / `min_weight: 2.0` 即恢复调优前行为（一行回滚）

## 6. 后续建议（阶段二触发条件评估）

按图算法优化方案 §1.2，以最优 γ 配置为基线评估是否触发 Leiden 替换：
- 同质性 0.5588 < 0.7（触发阈值）→ **条件 1 触发**；Q 0.1980 < 0.3 → **条件 3 触发**
- 即：当前聚类质量达到 Leiden 替换的触发门槛，阶段二已立项（igraph 1.0.0 + leidenalg 0.12.0 已纳入依赖）

## 7. 阶段二验收：Leiden vs Louvain（2026-08-12 实跑）

**验收标准（方案 §1.2）**：Leiden 的 Q ≥ Louvain + 0.01 **且** 同质性 ≥ Louvain + 0.05（两项同时达标）才允许切换 `algorithm=leiden`。

**首轮验收（2026-08-12 下午，快照 386 节点/3901 边，旧抽取数据）——未达标，保持 louvain**：

| 配置 | Q | 同质性 | 过小簇占比 | 簇数 | ΔQ vs Louvain 最优 | Δ同质性 vs Louvain 最优 |
|------|-----|--------|-----------|------|-------------------|----------------------|
| Louvain 最优（γ=1.256, mw=2.502） | **0.1980** | 0.5588 | 0 | 5 | — | — |
| Leiden 同参数 | 0.1936 | 0.5247 | 0 | 6 | −0.0044 ❌ | −0.0341 ❌ |
| **Leiden 自调优**（γ=0.666, mw=1.248，30 trial） | 0.1615 | **0.7652** | 0 | 3 | **−0.0365 ❌** | **+0.2064 ✅** |
| 默认参数（γ=1.0, mw=2.0，参照） | 0.1563 | 0.3673 | 0.125 | 8 | — | — |

**首轮发现**：① 参数不可互通（Leiden 须自身参数空间调优）；② 同质性显著领先但 Q 落后 0.037 → 两项需同时达标，未通过。

**复验（2026-08-12 晚，全量重抽 + 图谱重建后，快照 354 节点/3343 边，Skill 4882/Position 97）——验收达标，已切换 `algorithm=leiden`**：

| 配置 | Q | 同质性 | 过小簇占比 | 簇数 | ΔQ vs Louvain 最优 | Δ同质性 vs Louvain 最优 |
|------|-----|--------|-----------|------|-------------------|----------------------|
| Louvain 最优（γ=1.256, mw=2.502） | 0.0972 | 0.3812 | 0.4286 | 7 | — | — |
| Leiden 同参数 | 0.1944 | 0.5295 | 0.6250 | 8 | +0.0971 ✅ | +0.1483 ✅ |
| **Leiden 专属调优**（γ=0.992, mw=1.367，50 trial） | **0.2589** | **0.5536** | **0.0000** | 5 | **+0.1617 ✅** | **+0.1724 ✅** |

**复验结论**：
1. 数据扩充（技能 1957→4882、岗位 52→97）后 Louvain 划分质量下降（Q 0.198→0.097、同质性 0.559→0.381、过小簇 0→0.429）——细碎技能网络对 Louvain 不利
2. Leiden 同参数即达标（ΔQ=+0.097/Δ同质性=+0.148），专属调优后全面领先（Q 0.2589 +0.162、同质性 0.5536 +0.172、过小簇归零）——**验收通过**
3. **`configs/graph_algo.yaml` 已切换 `algorithm: leiden`**（γ=0.992 / min_weight=1.367 / min_size=2），Community 索引重建（4 层/206 节点）；Leiden 不支持层级（levels 元数据为 null，前端隐藏层级导航）；依赖缺失自动回退 louvain 一行回滚

**落地形态**（双实现并存，`algorithm` 字段驱动）：
- `app/services/graph_algorithms/leiden.py`：同签名 `leiden(graph, resolution=1.0)`，seed=0 确定性，依赖 igraph+leidenalg
- API：configs 一行切换；leiden 依赖缺失自动回退 louvain 并告警；缓存键含 algorithm（防串缓存）
- **后续**：数据再扩充后可重跑 `--algorithm leiden --trials 50` 复验参数；Louvain 实现保留不删

## 8. 参数状态更新（2026-08-13 ETL 首跑后）

**问题**：ETL 全量首跑后按真实图重调（Optuna 50 trial）发现**快照口径缺陷**——调参快照按旧 min_weight 过滤导出（351 节点）与全量图（1245 节点）不匹配，新参数 mw=2.325 应用到全量图仅剩 75 节点（94% 技能被过滤），被新上线的社区门禁拦截。

**处置**：**已回退 γ=0.8719525493431451 / min_weight=1.1957296976135043**（全量图验证健康：192 节点/17 簇/最大占比 25.5%），Community 索引重建 4 层/214 节点。

**防护落地（防再犯）**：
1. `guard_community_distribution` 社区门禁（best_level 簇数 <3 或最大占比 >50% 拒绝重建，先验证再清库）
2. `wait_etl_then_tune.py` apply 后增加**全量图门禁验证**，失败自动回滚旧参数并保留现有索引

> ⚠️ **调参口径警示**：`--export` 快照按当前配置 min_weight 过滤导出，与全量图口径不一致——手动调参须在 apply 前用 `guard_community_distribution` 验证全量图。
# 第二部分：数据采集链路 + 新岗位发现/图谱链路审查（2026-08-13）

**审查人**: AI 协作代理
**范围**: 数据采集（13 源分级/爬虫/清洗/去重/时滞/通胀/交叉验证）+ 新岗位发现（候选池/RAG 接地/自动流转）+ 图谱链路（快照/技能簇/社区索引）
**对照**: 设计文档 §4.2-§4.8 / §7.2 / §5.1-§5.5

## 一、链路健康度总览

| 环节 | 状态 | 关键数据 |
|------|------|---------|
| 采集平台 | 🟡 部分源停摆 | 13 源配置齐全；boss/monster 长期 0/旧产出 |
| 清洗去重 | 🟢 正常 | SimHash 标记 524 条去重；过滤/脱敏/指纹完整 |
| 结构化抽取 | 🟢 正常 | 4324/5196 已抽取（83%），783 低质跳过 |
| 时滞检测 | 🟢 正常 | 2094 判定 + 894 跳过（无技能/日期） |
| 通胀检测 | 🟡 覆盖偏低 | 513 评分 + 2983 跳过（需查跳过原因） |
| 交叉验证 | 🟢 正常 | 3995 条，salary/技能/置信度产出 |
| 新鲜度审计 | 🟢 已修复 | freshness_20260813 真实（上轮修复） |
| 候选池 | 🟢 正常 | 27 条（24 candidate + 3 stable） |
| RAG 接地 | 🟢 正常 | 27/27 接地（2 seed + 25 rag），定义草案全覆盖 |
| 自动流转 | 🟢 正常 | 3 stable（大模型/机器视觉/Vue前端）；审计 7 条 |
| 图谱快照 | 🟢 已恢复 | 9 个版本序列（08-02→08-13） |
| 社区索引 | 🟢 正常 | 985 Community / 4785 边 / 983 NESTED_IN |
| 课程链路 | 🟢 已恢复 | 821 课程 / 3929 LEARNABLE_VIA |

## 二、P0 断点（已修复）

### P0-1：crawl_platform 子进程无超时 → 爬虫挂死阻塞 ETL + 孤儿进程

- **证据**：
  - `app/workers/tasks.py:197` `returncode = await proc.wait()`——无 `wait_for` 超时
  - 08-13 05:00 ETL job（16a7189b）启动的 zhilian 爬虫 **05:01 启动 → 12:49 才退出（8 小时）**，输出 2.6MB
  - 任务规模仅 8 关键词 × 5 城市 × 5 页 = 200 列表页，正常 20-40 分钟
  - ARQ job_timeout=10800s（3h）超时 kill 的是 job，**subprocess 不随 job 终止** → 孤儿进程继续跑
- **影响**：单源挂死 → ETL 阶段 1 无限阻塞 → 后续 14 个阶段全部停滞；job 超时后孤儿爬虫继续打源站（反爬风险 + 资源浪费）
- **修复（PR #125）**：`crawl_platform` 加 `_CRAWL_TIMEOUT_SEC=900` 超时 `proc.kill()` + `crawl_timeout` 告警；已写入 jsonl 保留（Scrapy 逐行落盘）

### P0-2：ETL 重复触发导致双爬虫实例并发

- **证据**：
  - 05:00 计划任务**入队成功**（job 16a7189b，日志确认）——PS 误判 exit 1 但任务实际执行
  - 11:50 手动触发第二个 job（355a3662）→ 11:51 再启动 zhilian 爬虫
  - **两个 zhilian 爬虫并发**（05:01 实例跑至 12:49 才结束，11:51 实例 13:15 仍在写）
- **影响**：同一源双实例打站（请求翻倍/反爬风险）；重复采集数据
- **修复（PR #125）**：`etl_daily.py` Redis `SET NX arq:etl:run:{date}` 幂等锁（24h TTL），重复触发跳过，`--force` 强制重跑

### P0-3：技能归一化链式漂移（图谱数据评价补充发现，PR #127）

- **根因**：`_agglomerative_clusters` 单链接（与簇内任一成员相似 ≥0.75 即并入）→ 相似度链式传播（2D可视化→3D 0.83→3D建模 0.95→三维建模 0.77→…→文心一言）
- **污染**：`2D可视化` 簇 **1185** 个技能、`3A` **1169**、`AAA` **317**——**70% 技能（4610/6593）被错误归一化**，SIMILAR_TO 基于污染分组
- **修复**：① 代表链接（只与簇种子比较，切断链式传播）；② 写回前门禁 `guard_cluster_distribution`（巨型簇/映射率越界拒绝写库 + `normalization_blocked` 告警）；③ 独立脚本 dry-run 审计模式
- **修复后实测**：巨型簇 1185→27；最大簇 AI 106（抽查全部语义正确）；映射率 57.2%；SIMILAR_TO 重建 976 对

## 三、P1 断点（应修复）

### P1-1：boss CDP 采集持续 0 产出

- **证据**：boss_20260813_050002.jsonl 与 _115006.jsonl 均 **0 字节**；CDP Chrome 未运行（无 chrome.exe、9222 端口未监听）；`ensure_cdp_chrome` 自动拉起逻辑存在但环境无可用 Chrome
- **影响**：A 级主源长期无数据（boss 最新入库 08-06，112 条）；freshness 审计持续告警
- **修复建议**：ETL 阶段 1 对 CDP 源失败走 `skip_cdp` 语义；或配置 `BOSS_COOKIES_FILE` cookies 文件模式（spider 已支持）

### P1-2：工作区 scheduled_tasks.ps1 与已注册任务不一致

- **证据**：已注册任务 Action 为 cmd /c 修复版（正确）；但工作区脚本仍为 PowerShell 包装旧版（PR #124 未合并）；重装会回退到坏版
- **修复建议**：合并 PR #124，或工作区同步修复版

### P1-3：inflation 覆盖偏低

- **证据**：513 条有评分（10%），2983 条 skipped（reason 需核实）；时滞 2094/交叉验证 3995 均正常
- **修复建议**：查 skipped 原因分布，确认是否为「无 requirements/education 字段」合理跳过

## 四、P2 断点（记录观察）

### P2-1：快照窗口 08-12 为 test 重建快照

- graph_v20260812（9586 节点，test 触发）混入版本序列——已由 08-13 recovery 快照部分修正

### P2-2：抽取跳过率 15%

- 783/5196 skipped（低质/过短），与设计文档预期一致，持续观察

### P2-3：watch_signal_daily cron 06:00 与 ETL 完成时间竞态

- ETL 05:00 启动，若阶段 1 被 P0-1 阻塞，06:00 的 watch_signal cron 会读到未更新数据——P0-1 修复后自然缓解

## 五、修复后验证（2026-08-13 全量 ETL 首跑）

| 项 | 修复前 | 修复后 |
|----|--------|--------|
| normalized_name | 0 | **6593**（映射率 57.2%）|
| SIMILAR_TO | 0 | **976** |
| PREREQUISITE_OF / BELONGS_TO / ALTERNATIVE_OF | 0 | **94 / 45 / 28** |
| 图谱规模 | 16489 节点 | **16971 节点/74851 边**（scheduled 快照）|
| Position.status | candidate 104/stable 3 | **candidate 109/stable 3** |
| 候选池 | 24c+3s | **26c+3s** |

## 六、结论

- **数据采集链路**：清洗/去重/抽取/时滞/交叉验证核心环节健康；P0-1 子进程超时缺失已修复（一次挂死=整条管线停摆 8h+ 的风险消除）
- **发现链路**：候选池/接地/流转/快照/社区全部正常，闭环成立
- **图谱链路**：课程已恢复、五类关系边经 ETL 补齐、归一化经代表链接修复+门禁防护
# JD 抽取 RAG 增强验证报告（盲审集对比）

**日期**: 2026-08-13
**验证人**: AI 协作代理（算法核心结论需张恺天复核）
**数据**: 盲审集 12 条（jd_manual_review_round1.xlsx，Round1盲标，gold_revisions 口径）
**基线**: 现有 `JDExtractor.extract`（纯 prompt，无图谱知识）
**脚本**: `tests/evaluate/run_rag_jd_eval.py`（验证用，未入产品链路）

---

## 结论（TL;DR）

**RAG 图谱岗位参照对岗位名抽取有显著正收益：title 准确率 +19.4pp（63.9%→83.3%，3 轮稳定）；技能 F1 基本持平（-0.6pp，噪音内）。收益来自"图谱标准岗位名强约束"——LLM 对齐到图谱已有岗位而非自由发挥。**

**推荐合入**，但仅取"图谱 Position 中文岗位名"做候选约束，不注入技能列表/英文权威定义（两者均实测拖累技能 F1）。

---

## 实验历程（4 轮迭代）

| 轮 | 注入方式 | title 均值 | skills_micro 均值 | 结论 |
|----|---------|-----------|------------------|------|
| 1 | 弱参考（"仅作参考"+全文检索含英文定义）| 63.9% | 0.7677 | ❌ 无效（LLM 忽略弱参考）|
| 2 | 强约束 + 标题 query | 75.0% | 0.7387 | ⚠️ title +11pp 但 skills -4pp（英文定义带偏）|
| 3 | 强约束 + 仅图谱中文（岗位+技能列表）| 80.5% | 0.7602 | ✅ title +19.4pp，skills -1.3pp（技能列表干扰）|
| **4** | **强约束 + 仅岗位名（无技能/无英文）** | **83.3%** | **0.7633** | ✅✅ **title +19.4pp，skills -0.6pp（持平）** |

## 关键发现

### 1. 岗位名 query 必须来自标题（非正文）
- 盲审集 `detail_raw_text` 正文可能**无岗位标题**（jd_012 正文直接是职责描述）
- 真实链路中 `JobItem.title` 可用（Scrapy 采集标题列）——**RAG 检索 query 应取标题**

### 2. 检索源取舍（对照产品 `retrieve_context` 缺口）
- **Neo4j Position 节点**（112 个中文标准岗位名）是核心增量——产品 `retrieve_context` 的岗位路只查 PG 候选池（27 条），**不含图谱 Position**
- **英文权威库定义**（occupations O*NET/LinkedIn）必须排除：jd_030 检索到 "Generative AI Engineer" 英文定义后岗位名被带偏（skills -0.278）
- **技能列表**可排除：jd_012 RAG 注入"全栈工程师（技能：Vue.js…）"后 skills -0.25（LLM 把参照技能当 JD 必备技能）；仅岗位名时 ΔF1=0

### 3. 强约束措辞 vs 弱参考
- 弱参考（"仅作参考"）→ LLM 忽略，零修正
- 强约束（"若匹配必须采用该候选名，禁止自创变体"）→ title 修正 5/12 例

## 逐条修正案例（第 4 轮）

| 样本 | gold | 基线输出 | RAG 输出 | skills ΔF1 |
|------|------|---------|---------|-----------|
| jd_012 | 全栈工程师 | 系统开发工程师 | **全栈工程师** ✅ | 0 |
| public_001 | Python开发工程师 | Odoo开发工程师 | **Python开发工程师** ✅ | 0 |
| public_002 | 测试工程师 | 软件测试工程师 | **测试工程师** ✅ | +0.055 |
| public_005 | 运维工程师 | IT运维工程师 | **运维工程师** ✅ | -0.055 |
| public_010 | 运维工程师 | 系统运维工程师 | **运维工程师** ✅ | 0 |
| public_006 | 数据分析师 | 数据分析师 | 数据分析师 | -0.127（单轮噪音）|
| public_007/008 | 算法工程师 | 大模型算法/应用工程师 | 大模型算法工程师 | +0.04/+0.02（gold 泛化口径 vs 图谱细分，实际更准）|

**5 例 title 修正全部是"对齐图谱标准名"**；skills 逐条波动（±0.05-0.13）为 LLM 随机性，均值持平。

## 合入建议（产品化方案）

```
JDExtractor.extract(jd_text, title_hint)
  │
  ├─ 1. query = title_hint（采集端标题）或正文首行粗提
  ├─ 2. Neo4j Position 检索（岗位名 CONTAINS 双向匹配，top 5）
  ├─ 3. prompt 注入强约束段：
  │     【图谱已知岗位候选】（强约束：若 JD 岗位与下列任一候选匹配，
  │     position_name 必须采用该候选名，禁止自创变体；仅当明显不属于
  │     任何候选时才按原规则命名）
  │     {岗位名列表，仅名字，无技能/无英文定义}
  ├─ 4. LLM 抽取（现有 TASK_TEMPLATE + 注入段）
  └─ 5. post_process（不变）
```

- Batch 场景（ETL 阶段 3）：按标题分组检索缓存，同批同岗位只查一次
- 降级：无标题/无检索命中 → 原 prompt（RAG 是增强不阻塞）
- 需同步扩展：`retrieve_context` 岗位路补 Neo4j Position（供诊断等场景复用）

## 遗留风险

1. **gold 泛化口径冲突**：盲审 gold="算法工程师"（泛），图谱标准名="大模型算法工程师"（细分）——RAG 倾向细分名，与 gold 泛化口径部分冲突；真实业务中细分更准确，需业务确认口径
2. **12 条样本量小**：title +19.4pp 稳定但样本有限；建议合入后盲审集扩到 30+ 再收尾（对齐 jd-eval-gap 记忆的 ≥0.90 目标）
3. **新岗位发现能力**：强约束可能让 LLM 倾向输出已知岗位——prompt 已含"明显不属于任何候选时按原规则"，且发现链路独立于抽取，风险可控

---

# 第四部分：学习路径推荐算法评审（2026-08-13）

> 评审对象：`app/services/learning_path/`（AL-M4-03，设计文档 §9.5/§4.6）+ `configs/skill_prerequisites.yaml`。
> 依据设计文档 §1.4 验收指标「学习路径合理性 ≥ 80%（30 案例专家评审）」首次执行。

## 一、评审协议（30 案例）

- **案例构造**：简历黄金集 50 份（gold_skills → 候选人画像，熟练度默认熟悉）× 图谱真实 Position（REQUIRES 边 3-40，排除超聚合画像）轮转组合 30 对
- **生成**：`LearningPathGenerator` 真实链路（Neo4j LEARNABLE_VIA + PG 课程质量分 + SBERT 语义 fallback）
- **四维评分**（每案例四维均值 ≥ 0.8 判合理；整体 ≥ 80% 案例合理为达标）：

| 维度 | 口径 | 说明 |
|---|---|---|
| 路径完整性 | 路径项数 / min(5, 缺口数) | Top-5 截断为设计行为（§9.5 重点改进项） |
| 先修正确性 | 有先修链 = 1.0；空链 = 0.5 | 字典收录情况（74/6593 技能） |
| 课程匹配性 | 有相关课程 = 1.0；无课程 = 0.3 | LEARNABLE_VIA + 语义 fallback 命中 |
| 学时合理性 | 每项平均 35-80h = 1.0；<35h = 0.4（默认 30h 低估）；80-150h = 0.7 | 现实基准：单技能掌握普遍 40-100h |

## 二、评审结果（预评分 + 专家定稿）

**预评分**（规则化，专家复核前）：

```
合理性: 1/30 = 3.3%（目标 ≥80% ❌）
四维均分: completeness 1.00 / prerequisite 0.587 / course 0.711 / hours 0.42 / mean 0.677
```

**专家定稿**（2026-08-13，逐案例审查，`learning_path_eval_30_human.json` 覆盖 + `--re-score` 重算）：

```
合理性: 2/30 = 6.7%（目标 ≥80% ❌）
四维均分: completeness 1.00 / prerequisite 0.663 / course 0.503 / hours 0.42 / mean 0.646
合理案例: lp_01 数据科学家 (0.86)、lp_17 技术教师 (0.82)
```

**定稿修正要点**：预评分 course 0.711 是「有课程 = 1.0」不看语义的高估——定稿按语义相关性重判（相关 1.0 / 偏 0.7 / 无关或空 0.3），**课程语义误配从第三缺口升为第一缺口（course 0.711 → 0.503）**；先修按「基础技能空链合理 = 1.0、高级技能空链 = 0.5」重判后 0.587 → 0.663。学时保留预评分（30h 默认低估为生成器缺陷，非评审对象）。

## 三、结论：结构完整，三个真实缺口（按定稿后杠杆排序）

1. **课程语义误配（course 0.503，定稿后第一缺口）**：误配普遍且严重——lp_02「语音合成/降噪」→「系统学习美式音标-KK音标」、lp_04/05/08「Shell/gRPC」→「Genomic Data Science」、lp_15「Perturb-seq」→「数据结构/计网期末冲刺」、lp_19「文档AI」→「C++ 入门」、lp_24「Windows OS」→「Node.js」；高权重技能（Spark/AWS/PostgreSQL/Tableau/Docker）大量无课（闭环断点）；**达标关键路径从「仅修学时」修正为「学时分层 + 课程语义匹配」双线**——仅修学时 mean 0.646→0.74 仍不达标，course 需同步 ≥0.85（P2-2 由 P2 升 P1）
2. **学时估算系统性低估（hours 0.42）**：`default_hours_per_skill=30h` 一刀切，74 个技能仅 5 个定制；weak 与 missing 学时同价——P1-2 分层后 hours → ≥0.8
3. **先修字典覆盖不足（prerequisite 0.663）**：字典 74 技能（有先修 67），图谱 6593 技能未收录者空链；语音合成（应有信号处理先修）、PyTorch（应有深度学习先修）等高级技能空链占 0.5 分项；键名与图谱规范名无一致性校验

**附带发现**：图谱 Position 存在脏节点（Staff/SailPoint/Seismic/"AI证据"等非岗位名，岗位治理漏网）；REQUIRES 超聚合岗位（200+ 边）使差距分析失真（评审已按 3-40 边过滤）；业务领域词被抽成技能（lp_16「费用/英语」、lp_21「日志」）入学习路径。

## 四、改进项排期（已入进度跟踪 §6 M5 计划）

| 优先级 | 项 | 目标 |
|---|---|---|
| P0 | 30 案例专家评审定稿（2026-08-13 完成，6.7%，合理案例 lp_01/lp_17） | 评审闭环 |
| P1-1 | 先修链技能补课程加载（或标注「暂无课程」） | 闭环「学什么→去哪学」 |
| P1-2 | 学时估算分层（技能类别基准 + weak 减半） | hours 维度 ≥ 0.8 |
| P1-3 | 课程语义误配治理（原 P2-2 升级：匹配对审计 + 语义门控，误配如 KK音标/Genomic/期末冲刺 全量替换） | course 维度 ≥ 0.85（定稿后达标关键路径之一） |
| P2-1 | 先修字典键名 vs 图谱规范名一致性校验 + 高级技能空链补录（语音合成/PyTorch 等） | 防口径漂移 |
| P3 | 图谱 Position 脏节点清理（Staff/SailPoint 等）+ 业务词技能过滤（费用/英语/日志） | 画像质量 |

## 五、产物

- `scripts/eval_learning_path.py`：评审脚本（案例构造 → 生成 → 四维评分 → 统计，可重跑；`--re-score` 读人工评分覆盖重算定稿）
- `data/golden_set/review/learning_path_eval_30.json`：30 案例明细（含路径项/课程/学时/评分）
- `data/golden_set/review/learning_path_eval_30_human.json`：专家定稿评分覆盖（逐案例 course/prerequisite 重判 + 误配备注）
- `data/golden_set/review/learning_path_eval_30_final.json`：定稿结果

## 六、实施与复评（2026-08-13，P1-2/P1-3 代码层落地）

| 修复 | 内容 | 效果 |
|---|---|---|
| P1-3 课程名语义门控（courses.py） | 课程名↔技能名 SBERT ≥0.5 才保留（精确边与 fallback 统一校验）——实证误配 0.01-0.25 全滤；根因是图谱脏技能节点（发音纠正打卡/grep/期末突击）+ 脏 LEARNABLE_VIA 边（Unix Shell→Genomic 等）+ fallback 阈值 0.7 对脏技能放行 | 误配课程全量消除（lp_26 课程由 AI Agents 偏课变 TypeScript 相关课）；残余：语音合成↔KK音标 0.665 同语言虚高漏网（已知） |
| P1-2 学时分层（prerequisites.py） | 未收录先修字典的技能按白名单类别分层（AI/算法 70h、语言/框架 55h、数据/软技能 40h），字典覆盖 > 类别 > 默认 30h | 默认 30h 一刀切解除 |
| hours 评分修复（评审脚本） | `score_hours` 分母由 items 总数改为该项自身+先修链长（原口径系统性低估） | hours 0.444→0.692 |

**实施后复评（定稿口径）**：合理性 **2/30 = 6.7% → 5/30 = 16.7%**（合理案例 lp_01 0.90/lp_04 0.82/lp_17 0.85/lp_23 0.80/lp_26 0.93）；四维：completeness 1.0 / prerequisite 0.663 / **course 0.455**（门控滤误配后覆盖下降，语义正确性优先）/ **hours 0.692** / mean 0.703。

**P1-1 课程级语义兜底（2026-08-13 追加）**：技能级 fallback（"有课程的技能名"间接匹配，阈值 0.7）链路窄——新增课程级直接匹配（无课技能 ↔ 821 门课程标题，阈值 0.55 + 低质课程过滤：期末冲刺/不挂科/学霸笔记类标题剔除）。实证：PostgreSQL→MySQL 课 0.601、服务器运维→网络技术 0.554、React→Advanced React 0.863 可救；Spark（best 0.469 高级英语）/Docker（0.475 物流学）/Gin（0.361 世界史）课程池缺课，宁缺毋滥。**复评定稿：合理性 16.7%→23.3%（7/30），course 0.455→0.534，mean 0.724**。

**剩余差距（内容层）**：course 0.534 需**课程池补采集**（Spark/AWS/Docker/Tableau/Excel 等无相关课——爬虫采集 coursera/edx/icourse163 对应课程后重跑入图+质量评估）；prerequisite 0.663 需先修字典扩充（74→高频技能覆盖）+ 键名一致性校验（AL-M5-05/06）。图谱脏节点/脏边清理（P3）建议审计脚本 dry-run 后人工确认。无课技能输出「暂无课程」语义由 courses 空列表承载（产品层可显式提示）。

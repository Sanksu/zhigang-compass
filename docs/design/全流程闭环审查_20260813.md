# 全流程闭环审查报告（2026-08-13）

> 项目编号：XH-202621 · 智岗罗盘
> 审查时点：2026-08-13 01:55（UTC+8）
> 审查方式：只读代码级 + 真实库验证（PostgreSQL jd_raw 4324 行 / Neo4j / Redis db1）
> 四链路基准：设计文档 §0.1 系统总览 + 评分维度「作品完整性 30 分」

---

## 一、总览

| 链路 | 代码闭环 | 运行时状态 | 关键断点 |
|------|---------|-----------|---------|
| 一、数据采集（§4） | ✅ 15 阶段 ETL 编排完整 | ✅ **调度已恢复**（02:30 修复，首跑 05:00） | worker 已拉起 + 计划任务已注册 |
| 二、新岗位发现 + 图谱（§5/§7） | ✅ 六态机/图算法/接地完整 | ⚠️ **状态断** | 图谱重建重置 Position.status + 参数失配（P1 待办） |
| 三、匹配诊断（§8/§9） | ✅ 上传→解析→匹配→诊断→反馈全链 | ✅ **课程链已恢复** | 课程 821 门已重导入图（LEARNABLE_VIA 3929） |
| 四、前端 + 测试/评测 | ✅ 12 页真实 API / E2E / 评测 / CI / 部署 | ✅ 闭环 | 小缺口（.env.example 缺 ARQ_REDIS_URL） |

**总体结论**：代码层面四链路完整闭环；运行时存在 **3 个实质性断点**（下述 A/B/C），根因均为「08-11 后自动化链路停摆 + 08-12 手动图谱重建的副作用」，非设计缺陷。

---

## 二、链路一：数据采集（⚠️ 调度断）

**✅ 已闭环**：
- 13 源爬虫齐备（7 源自动采集 + 5 源 cron + monster 停采）；容器化（worker 挂载 output）
- 清洗管线（指纹/脱敏/SimHash/质量评分/时效加权）新记录全覆盖（quality 覆盖 74.6%）
- 入库幂等：(source, source_id) 重复 0、fingerprint 唯一 4324/4324
- 时滞/通胀/交叉验证真实生效（fresh 1593 / 通胀 mild 198+severe 13 / 交叉验证 verified 3589）

**🔴 断点 A（根因）**：
1. **ARQ worker 08-11 20:08 宕机未拉起**（Redis ConnectionError 后无日志）→ ETL/独立 cron/LLM 健康检查/观察池 cron 全停摆；Redis db1 积压 10 个 match_recommend
2. **Windows 计划任务未注册**（schtasks 无 Zhigang 任务）→ 05:00 ETL 从未自动触发，08-08 起 T+1 违约（最近 scheduled 快照停在 08-07）
3. 新鲜度审计失真：`reports/freshness_*.json` 与真实库矛盾（真实 7 源全 stale）

**⚠️ 次要点**：boss/monster 老记录无 quality 标记（口径不一致）；单日采集 P0≥100 条当前不可达成。

## 三、链路二：新岗位发现 + 图谱（⚠️ 状态断）

**✅ 已闭环**：
- 图谱构建：抽取 3541/4324（81.9%），Position 107 / Skill 5944 / Evidence 5660；聚合生效（REQUIRES.weight avg 0.426）
- 图算法：`algorithm=leiden`（复验达标 ΔQ+0.162）+ Community 索引 4 层 206 节点
- 发现链路真实产出：候选池 27 条（candidate 24 + stable 3，Z-score 门控生效）；RAG 接地 27/27 rag_matched
- 状态机六态代码完整（含 declining→stable 回迁）

**🔴 断点 B（图谱重建副作用）**：
1. **08-12 21:55 手动 test 重建图谱重置全部 Position.status='candidate'**（含 PG 中 3 个 stable）→ 前端按 Neo4j 渲染全显示 candidate，状态持久化断裂
2. **skill-clusters 参数失配**：leiden 参数基于 354 节点验收快照，现图 957 节点 → live 只出 4 巨型簇；Community 索引未随图增长重建

**⚠️ 次要点**：观察池停更（08-10）；RAG 权威库仅 O*NET 单源（hrss/linkedin 未导入）；Skill 归一化未生效（normalized_name=0/SIMILAR_TO=0）。

## 四、链路三：匹配诊断（⚠️ 数据断）

**✅ 已闭环**：
- 上传→解析（pdf/docx/OCR/双栏 reflow）→ PII 脱敏→LLM 抽取→落库：resume_cache 4 条、task 13 次
- 匹配：match_recommend 72 任务、match_results 82 行、Redis+PG 双写
- 诊断：call_sync（G-04 已接线）、24h 缓存、证据引用拦截、错误码 503/504
- 反馈端点完整（Redis List TTL 90 天）

**🔴 断点 C（课程入图丢失）**：
1. **Neo4j Course=0 / LEARNABLE_VIA=0**（PG course_raw 821 门 + evaluate_courses 505 条质量分在）——08-03 ETL 曾导入 505 门，08-12 rebuild_graph 全量清库后未重导 → **学习路径课程推荐实时返回空**（gap→path 后半段失效）
2. 图谱先修/相似/演化边全 0（PREREQUISITE_OF/SIMILAR_TO/EVOLVED_FROM/BELONGS_TO/ALTERNATIVE_OF）——先修链依赖字典兜底（可运行）
3. match_feedback PG=0（Redis 4 条，落库失败静默）

**⚠️ 次要点**：generator.py docstring 陈旧（代码已 call_sync）；诊断/反馈样本量小。

## 五、链路四：前端 + 测试/评测（✅ 闭环）

- 前端 12 页全部真实 API（抽查 graph/resume-match/admin/evolution 无业务 mock）；算法面板（PageRank/技能簇+层级/最短路径/社区树）全接真实端点
- E2E 全流程（登录→图谱→匹配→后台→登出）
- 评测体系：四任务 + 盲审归档链（0.6749→0.8416）+ 黄金集（jd 100/简历 50/匹配 300/盲审 12）
- 测试基线：后端 1234 用例（全量 1226 passed 0 失败）/ 前端 29 文件 127 用例 / CI 双 workflow
- 部署：5 服务 + healthcheck + entrypoint 自动迁移；`.env.example` 缺 ARQ_REDIS_URL（有默认值，低风险）

## 六、断点修复状态（P0 已完成 2026-08-13 02:31）

- **P0-1 worker 拉起 ✅**：本机 `uv run arq app.workers.tasks.WorkerSettings` 运行中（23 函数 + 2 cron 注册；容器构建因 Docker Hub 网络超时改用本机形态，功能等价）；积压 10 个 match_recommend 全部消费（match_results 82→92）；cron 健康检查正常（deepseek ✓/spark/qwen 降级）
- **P0-2 计划任务注册 ✅**：scheduled_tasks.ps1 加 UTF-8 BOM 修复 PS5.1 编码解析后注册 6 任务（ETLDaily 05:00 / CrawlMaimai 23:00 / CrawlLinkedIn 08:00 / 课程周日 10:00-11:00），schtasks 全部"就绪"
- **P0-3 课程入图 ✅**：load_courses + evaluate_courses 重跑——Neo4j **Course 821 / LEARNABLE_VIA 3929**（08-03 导入后重建丢失的数据恢复），学习路径课程推荐闭环恢复；evaluate_courses 68 门推荐

## 七、断点修复状态（P1 已完成 2026-08-13 03:02，经 PR 重放合入）

- **P1-1 状态回写 ✅**：3 个 stable 候选（大模型算法工程师/Vue前端开发工程师/机器视觉算法工程师）按 PG 候选池同步回写 Neo4j Position.status（persist 幂等；历史快照不含 status 无法恢复其他岗位，且非候选池岗位重建前亦为 candidate——与重建前一致）
- **P1-2 参数复验 ✅**：现图（3930 节点/101 万边）重跑 leiden 专属调优 → 最优 γ=0.872/mw=1.196（同质性 0.4064→0.5824，objective 0.4158→0.4675）写回 configs；Community 索引重建（5 层/985 节点/983 嵌套边）；当前参数下 Leiden 达标（ΔQ+0.047/ΔH+0.091）
- **P1-3 feedback 落库 ✅**：落库链路正常（实测 0→1 行，测试记录已清理）——PG=0 根因是 08-04 反馈提交时 match_feedback 表尚未创建（后建迁移），异常被静默；表存在后新反馈正常落库

## 八、断点修复状态（P2 已完成 2026-08-13 03:30）

- **P2-1 新鲜度审计 ✅**：重跑 check_data_freshness → reports/freshness_20260813.json 真实审计恢复（多源 1.5-6 天 stale 如实报告 + 告警正常触发，与真实库一致——此前报告文件失真因演示/测试库覆盖）
- **P2-2 观察池/快照回补 ✅**：watch_signal_daily 产出 6 信号（technology_watch 更新）；snapshot_graph 生成 graph_v20260813（manual-recovery）——T+1 快照链恢复；worker cron（watch_signal_daily 06:00）明日自动续跑

## 九、断点修复优先级（原建议，P0/P1/P2 已全部执行）

| 优先级 | 断点 | 修复动作 | 归属 |
|--------|------|---------|------|
| P0 | A-1 worker 宕机 | 拉起 ARQ worker（`uv run arq app.workers.tasks.WorkerSettings`），积压任务自动消费 | 后端/运维 |
| P0 | A-2 计划任务未注册 | 安装 `scripts/cron/scheduled_tasks.ps1`（05:00 ETL + 各爬虫 cron） | 后端/运维 |
| P0 | C-1 课程入图 | 重跑 ETL 阶段 6（load_courses + evaluate_courses）恢复 LEARNABLE_VIA | 数据 |
| P1 | B-1 状态重置 | 重跑 ETL 阶段 15（discovery_auto_transition）回写 Position.status | 算法/后端 |
| P1 | B-2 参数失配 | 现图（957 节点）重跑 `graph_algo_tune.py --algorithm leiden --trials 50` + sync_communities 重建索引 | 算法 |
| P1 | C-3 feedback 落库 | 排查 match.py:540 落库失败（表结构/权限），补齐 PG 数据 | 后端 |
| P2 | A-3 新鲜度审计失真 | 重跑 check_data_freshness 恢复真实审计 | 数据 |
| P2 | 观察池/快照停更 | worker 恢复后自动回补（watch_signal_daily + snapshot_graph） | 后端 |

## 七、结论

- **作品完整性四链路代码级闭环，无设计断点**；演示路径（图谱/匹配/诊断/前端）可用
- 运行时 3 个实质性断点（worker 宕机、计划任务缺失、课程入图丢失）均为**运维/数据恢复类**，非代码缺陷；修复后 T+1 承诺与学习路径闭环恢复
- 发现链路状态需在 ETL 恢复后重跑阶段 15 回写

## 八、P0 修复记录（2026-08-13 02:30 执行）

| 项 | 断点 | 修复动作 | 结果验证 |
|----|------|---------|---------|
| A-1 | ARQ worker 宕机（08-11 20:08） | 后台拉起 `.venv python -m arq app.workers.tasks.WorkerSettings`（Start-Process 隐藏窗口，日志 `backend/logs/arq_worker.log`） | ✅ 23 functions 注册；`arq:queue:health-check` 心跳恢复；**10 个积压 match_recommend 被启动恢复机制自动重新入队消费**；OCR 预热正常 |
| A-2 | Windows 计划任务未注册 | 根因修复：`scheduled_tasks.ps1` 无 BOM UTF-8 → PowerShell 5.1 按 GBK 解码致 ParserError（PR #121）；加 BOM 后注册 | ✅ 6 任务全部 Ready：ETLDaily 05:00（今日首跑，T+1 恢复）/ CrawlMaimai 23:00 / CrawlLinkedIn 08:00 / 课程源周日 10:00-11:00 |
| C-1 | 课程入图丢失（Course=0） | 直接调用 `load_courses` + `evaluate_courses`（幂等 MERGE，脚本 temp/recover_courses.py 不入库） | ✅ Course **821**（此前 0）/ LEARNABLE_VIA **3929** 条边（此前 0）/ 587 技能挂课；质量评估 821 门、**推荐池 68 门**（Top1 Python for Everybody 0.712） |

**修复后状态**：链路一、三运行时断点解除；观察池/快照回补随 worker 恢复自动进行（watch_signal_daily 06:00 cron + snapshot_graph）。剩余 **P1**（B-1 status 回写、B-2 参数重调、C-3 feedback 落库）与 **P2**（A-3 新鲜度审计）待后续窗口处理。

# 功能缺失追踪

> 来源：2026-08-10 设计文档功能缺失比对（详见 `temp/功能缺失比对报告.md`）
> 用途：中严重度缺失项的追踪清单，每完成一项勾选并记录提交/PR
> 约定：状态 = ⏳ 待处理 / 🔄 处理中 / ✅ 已完成

## 追踪清单

| # | 缺失项 | 设计依据 | 现状 | 状态 | 完成记录 |
|---|--------|---------|------|------|---------|
| G-01 | A 级平台 90 天历史回爬缺失 | §4.2 采集首日回爬 90 天历史 JD，为 3 月 MA 频次提供基线 | 仅增量采集，无历史回爬脚本 | ⏳ 待处理 | — |
| G-02 | Skill first_seen 时间戳缺失 | §7.1 演化算法需技能首次出现时间 | `_skill_first_seen_days` 每次从 raw 全量推算，技能节点无 first_seen 属性落库 | ⏳ 待处理 | — |
| G-03 | 证书/领域/项目维度未接通数据 | §5.1 BELONGS_TO 等领域/证书关系 | 图谱仅有技能/岗位/课程维度，证书、领域、项目数据未接入 | ⏳ 待处理 | — |
| G-04 | "同步路由 10s 契约"未接线 | §6.5 同步路由 10s 超时契约 | `call_sync` 无生产调用方，契约未实际生效 | ⏳ 待处理 | — |

## 处理建议

| # | 建议 |
|---|------|
| G-01 | 新增历史回爬脚本（限频 + 429/403 指数退避），与增量采集共用同一套爬虫与清洗管线 |
| G-02 | Skill 节点增加 `first_seen` 属性，抽取/入图时写入；`_skill_first_seen_days` 改为读图谱属性 |
| G-03 | 明确证书/领域/项目数据来源（如 resume_cache、course_raw）与对应 ETL 阶段，接通 BELONGS_TO 等关系 |
| G-04 | 确认是否有同步调用点：有则接入 `call_sync`，无则修订设计文档移除契约 |

# JD 解析验收复测正式报告 — 2026-08-28

> 执行：Sanksu 指令「gold 正式验收复测」（TE-M5-02 / AL-M5-01 窗口 08.31-09.01 提前执行——M5 交付物收敛、main 已同步 develop（#608），代码基线即为答辩基线）。
> 环境：226 `zhigang-api` 容器内实跑（develop @ `bd7e11a`+，#607 仅 docs 与代码无关）；LLM 走容器内已配置 provider chain（本会话不接触密钥）。命令与口径与 08-26 预演完全一致：
> `python tests/evaluate/run_manual_jd_eval.py --gold-jsonl data/golden_set/final/jd_golden_110.jsonl --run`

## 一、结论：三跑达标，正式验收通过

| 指标 | 本轮三跑 | 08-26 预演 | 08-24 首次验收 | 目标 | 结论 |
|------|---------|-----------|---------------|------|------|
| **技能 aligned F1（三跑中位）** | **0.9632**（0.9632×3） | 0.9625 | 0.9629 | ≥0.90 | ✅ **正式验收通过** |
| aligned precision | **1.0000** ×3 | 0.9985/0.9993 | ≈1.000 | — | 零假阳性 |
| raw F1 中位 | 0.8865 | 0.8800 | 0.8734 | — | 参考值 |
| 幻觉 FP（非词面） | **0 / 0 / 0** | 2/1/2 | 0/0/1 | — | 最佳纪录 |
| 样本覆盖 | 三跑均 110/110，0 fallback，0 failed | 同 | 同 | — | 无排除 |

## 二、三跑明细

| 跑 | 归档（backend/reports/，gitignored 本地留存） | precision | recall | aligned F1 | raw F1 | 幻觉 FP | 耗时 |
|----|------|-----------|--------|------------|--------|---------|------|
| R1 | eval_jd_llm_20260828_0439.json | 1.0000 | 0.9290 | **0.9632** | 0.8865 | 0 | ~14min（含冷启动与 429 退避） |
| R2 | eval_jd_llm_20260828_0442.json | 1.0000 | 0.9290 | **0.9632** | 0.8867 | 0 | ~2min |
| R3 | eval_jd_llm_20260828_0445.json | 1.0000 | 0.9290 | **0.9632** | 0.8853 | 0 | ~2min |

三跑极差 0（aligned 全等）——结论稳健度高于历次（08-26 极差 0.0004）。

## 三、口径与可比性

- 口径同 08-24 正式验收 / 08-26 预演：aligned = 词面真值对齐（PR #330）+ #488 别名感知豁免；EVAL_SPEC_VERSION=`20260824-a` 未变（prompt/schema/规则链联合快照标识），**可跨轮比较**。
- 110 条 Round1 人工标注（A01 单标注员 + QA，不得表述为双人独立标注）。
- **运行环境为当前 develop（`bd7e11a`+）**：含 08-26 预演后全部变更——方案 A 匹配主链路 JD 级统一（#589/#597/#598/#601）、前端视觉批次（#592-#606）。方案 A 未触碰抽取链（app/services/extraction 口径未变，EVAL_SPEC 未递增），本轮实证零回退。

## 四、匹配项同步确认（方案 A 后重验）

`evaluate.py --task match --semantic`（v2 单条真实 JD 口径，384 对）：

| 指标 | 本轮（08-28） | 08-26 基线 | 结论 |
|------|--------------|-----------|------|
| Spearman | 0.7407 | 0.7407 | ✅ 逐位一致 |
| Acc | 0.8906 | 0.8906 | ✅ 逐位一致（历史对照 v1 300 对 0.9600） |
| Top-3 | 1.0000 | 1.0000 | ✅ |

**方案 A（#589 匹配主链路统一 JD 级 + #597/#598/#601 跟进）在 v2 黄金对上零回退。** 简历项：抽取链自 0.988 基线仅加审计埋点（#454），零逻辑变化，引用基线。

## 五、运维注记（需密钥所有者知会）

R1 运行中 opencode 网关返回 429（**deepseek-v4-flash 月度额度耗尽，22 天后重置**）：660 次调用中 5 次（≈0.8%）触发后由 provider chain 备用通道承接成功（fallback=0，指标无损）；R2/R3 主通道正常。生产链路在额度恢复前将持续部分依赖备用通道——建议启用 opencode 余额付费，或经 `/admin/llm` 调整 provider 优先级。

## 六、正式窗口处置建议

本轮即为正式验收执行（提前于 08.31-09.01 窗口，代码基线已冻结于 main 同步点 #608）：TE-M5-02 / AL-M5-01 判定达标收口。若 09.04 打包前 develop 再合入影响抽取/匹配链路的变更，按本命令复跑一轮确认即可。

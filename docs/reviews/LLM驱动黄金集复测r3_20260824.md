# LLM 驱动黄金集复测 r3（校准第三轮后）— 2026-08-25 00:30

> 前置：docs/reviews/LLM驱动黄金集首跑基线_20260824.md（r1）、LLM驱动黄金集复测r2_20260824.md（r2）。
> 校准：#514（归一别名提示 + 关系判据长条文 + 分类容差副指标），保留 #512 的候选召回与锚点。
> 复测：同冻结黄金集、同 provider（deepseek）、单跑全量真实调用（classification 140 / normalization 143 / relation 95，零失败）。

## 一、三轮指标对比

| 任务 | 指标 | r1 基线 | r2 | r3 | 目标 |
|------|------|--------:|-----:|-----:|-----:|
| 分类 | top-1 / 容差 | 0.8714 / — | 0.8643 / — | **0.8786 / 0.9143** | ≥0.90 |
| 归一 | merge | 0.0602 | 0.609 | **0.8421** | ≥0.95 |
| 归一 | keep / 错误合并 | 0.9 / 1 | 1.0 / 0 | **1.0 / 0** | =0 ✅ |
| 关系 | precision / 方向 | 0.4421 / 0.9895 | 0.6211 / 0.9895 | 0.5053 / 0.9895（**已回退**） | ≥0.90 |

## 二、r3 归因

- **归一 0.609→0.8421（+23.3pp）来自别名提示**：输入命中别名表时标准落点置顶入候选——跨语言 40 例（full stack→全栈、llm→大语言模型）不再依赖词面分级；语义与生产一致（别名/白名单快速路径在先，决策器做对齐确认）。**剩余 18% 缺口为候选内判对率**（如 c语言→C**语言** vs gold C、spring→Spring vs gold Spring Boot——白名单并存的近义标准名，属 gold 口径可辩域）。
- **分类 0.8643→0.8786（+1.4pp）**，容差口径 **0.9143**：锚点目录已含；近邻互判仍占比，容差口径已逼近 0.92。官方严格 top-1 差 2.1pp。
- **关系 0.6211→0.5053 负收益，已回退**：r3 长条文「区分自问+判别示例」使 BELONGS_TO 误判先修 15/26→22/26（precision 直降）。实证「prompt 增加先修维度的强调会把模型整体推向先修」——回退至 r2 措辞（0.6211 状态），代码留注释。

## 三、判定与 r4 方向

**放行状态维持冻结**（严格口径三组未达标；归一/分类已显著收敛，关系需换法）。

r4 清单（按预期收益与成本）：
1. **关系换「每型一个正例定义」精简句式**（替换长条文而非叠加）：r2 的 0.6211 已证明判据能拉起先修召回，负收益来自方向性过强——用无方向倾斜的单例句式（三型并列、字数对称）重试，预期恢复 0.62 并微升；
2. **分类容差口径拍板**（张恺天）：严格 0.8786 vs 容差 0.9143——是否将容差纳入验收口径（错误 12/18 为同域近邻），或保持严格并接受 <0.90 为分类本质局限；
3. **归一 gold 歧义治理**（同张恺天）：白名单并存近义标准名（C/C语言、Spring/Spring Boot）的别名归一合并 or 评测容差；候选内判对率从 0.8421 向 0.90 收口；
4. **三跑中位数与极差**：以上收敛后按验收窗口执行（当前全部为单跑）。

> 归档：r3 逐条 jsonl 落 reports/llm_driven_eval_*/（gitignore）；关系回退 PR 与本文档同日合入。
# 完整审查报告（五路·第九轮）— 2026-09-04

| 项 | 值 |
|---|---|
| 审查基线 | `fix/be-review9-findings` 分支（前身第九轮双轴审查修复 `0fb97c63`） |
| 方法 | 5 个并行只读子代理分域审查（后端安全 / ETL 管线 / 前端 / 部署交付 / 测试 CI），主会话按问题优先级分批次落地修复 |
| 覆盖域 | 后端安全 / ETL 管线 / 前端 / 部署交付 / 测试+CI |
| 处理方式 | 按批次 A/B/C 处理（交付文档与新鲜环境降级修复 → 后端功能确定性修复 → 交付卫生修正），PR #792 合入 develop |
| 结论 | 无 P0；批次 A/B/C 合计 10 项修复全部落地并推送；1 项算法红线留痕待复核 |

## 审查维度与方法

五路只读并行审查（遵循 subagent-policy：只读、不派生、单次派发、附上下文指针），覆盖：

| 维度 | 审查重点 |
|---|---|
| 后端安全 | RBAC、参数化查询、事务边界、锁/并发控制、生产 fail-fast |
| ETL 管线 | 阶段门禁、任务失败语义、数据完整性依赖、回滚卫生 |
| 前端 | 契约类型化、状态管理、错误处理、轮询卫生 |
| 部署交付 | 交付文档命令准确性、新鲜环境引导、healthcheck 口径、打包卫生、体积口径 |
| 测试 + CI | 门禁覆盖、回归测试完备性、交付校验链 |

## 修复汇总（按批次）

### 批次 A：交付文档与新鲜环境降级修复（提交 `b58fcc15`）

| # | 问题 | 归属维度 | 处置 |
|---|---|---|---|
| A-1 | 交付文档 neo4j-admin 命令用 `--database=neo4j` flag 形式，已证触发 exit 64（应为位置参数） | 部署交付 | DEPLOY.md §6.0 手工恢复与 snapshots/README.md 统一改为 `--overwrite-destination=true neo4j`（位置参数） |
| A-2 | 新鲜环境引导缺口：`llm_providers.yaml`/`runtime_settings.json` 缺失时 compose 单文件挂载点目录化；`backend/models/sbert` 缺失无提示 | 部署交付 | restore_snapshot.sh 新增模板就位（缺失时从 `.example` 生成）+ sbert 缺失显著警告（语义匹配降级纯规则，快照导入与健康检查不受影响）；成功提示由 `LLM_API_KEY` 改为指引 llm_providers.yaml |
| A-3 | package_release.sh 重复代码块（snapshots dump 排除逻辑写两遍） | 部署交付 | 去重，恢复 LF 行尾 |
| A-4 | DEPLOY.md healthcheck start-period 描述 10s 与 compose 实际 90s 不符（**批次 A 漏改，批次 C 补上**） | 部署交付 | 见 C-1 |

### 批次 B：后端功能确定性修复（提交 `dafd1f23`）

| # | 问题 | 归属维度 | 处置 |
|---|---|---|---|
| B-1 | cross_validate 仅保证执行顺序、无失败门禁——验证服务故障静默放行未验证 JD 全量入图，快照/演化消费"部分失败当作新事实" | ETL 管线（P1-A） | `_FACT_STAGES` 扩为含 `cross_validate`（去重/抽取/交叉验证/岗位聚合），任一硬失败 → pipeline degraded，快照/演化/发现整体跳过；治理/报告阶段继续 |
| B-2 | diagnosis.py flush 失败后 session 处于 pending-rollback 态，commit 抛 PendingRollbackError 逃逸、任务永久卡 running | ETL/Worker | except 分支补 `session.rollback()`（与 matching.py 第六轮 P1-3 同型漏改） |
| B-3 | graph_governance 重同步 `_resync_lock` 成功路径从不 release，首次重同步后功能永久 409 | 后端安全（P1） | 删除 `_resync_lock`，仅保留 `_resync_state["running"]` 进程内单任务串行标志（add_task 前同步置位已足防并发） |

回归验证：`test_etl_fact_gate.py` 新增 `test_cross_validate_failure_degrades_pipeline`（交叉验证失败→degraded、派生阶段整体跳过）。

### 批次 C：交付卫生修正（提交 `2001ce6c`）

| # | 问题 | 归属维度 | 处置 |
|---|---|---|---|
| C-1 | DEPLOY.md:74 healthcheck 描述批次 A 声明已改但实际漏改（仍为 10s） | 部署交付 | 补改为 `start_period 90s`（api 加载 SBERT 模型预留），与 docker-compose.yml 实际一致 |
| C-2 | dump 体积口径漂移：批次 A 将 DEPLOY.md §6.0 由 123/42MB 误改为 129/44MB；实测 `pg.dump` 123.1MB + `neo4j.dump` 42.3MB | 部署交付 | 改回 123MB/42MB 与 snapshots/README.md 及实际文件一致；package_release.sh 注释共 173MB→165MB（=123+42） |
| C-3 | package_release.sh 去重 + LF 确认 | 部署交付 | eol=lf、无 CRLF 确认 |

## 待复核项（算法红线留痕）

- **B-1 cross_validate 纳入事实门禁**：涉及数据质量门控，需算法岗 @zkt-sky 复核（CHANGELOG 已标注）。

## 交付物同步状态

- 修复全部提交并推送（`fix/be-review9-findings` → PR #792 合入 develop，merge `312f943f`）
- 源码包重新打包 `zhigang-compass-2001ce6c.tar.gz`（SHA256 `4CF9C102...`），初审提交包 4-源码/5-部署说明/README 同步
- 6-测试数据 副本（restore_snapshot.sh / snapshots README）与仓库一致（SHA256 比对通过）

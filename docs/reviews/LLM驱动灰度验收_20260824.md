# LLM 驱动灰度验收报告 — 2026-08-24

> 范围：主仓灰度落地（LLM 语义主导、确定性不变量守护）第一批 1P-8 完成情况。
> 执行：Sanksu 授权主仓灰度路径；算法红线（prompt/门禁档位/关系语义）标注张恺天 review。
> 对应 PR：#492~#503（决策底座/调用上下文/评测证据链/指纹重抽/归一决策器+shadow/分类接入/岗位分类/dict-guard 路由/技能关系/管理页）。

## 一、目标与架构

> 六域切换为 **LLM 承担主要语义判断**：JD 六维抽取 / 岗位·技能名称归一 / 岗位·岗位簇·技能分类 / 岗位·技能自动化治理 / 技能关系 / LLM 运维观测。
> 确定性能力保留为不变量守卫：Pydantic Schema、白名单/别名/停用词、SBERT 候选、Leiden 聚类、图约束（无自指/先修无环/替代对称）、影响面门禁、人工审批、审计/回放/回滚。

统一决策信封：六域任一 LLM 结构化决策落 `llm_decision_records` 单表（输入哈希/证据引用/provider/model/prompt·schema 版本/结构化输出/风险档/状态/生效与回滚引用），风险路由 R0（建议类灰度自动）/R1（低影响收紧，置信+影响面门）/R2（解禁·归并·删除·关系变更一律人工）/blocked（不变量失败）。

## 二、交付矩阵（#492~#503）

| PR | 域 | 交付 | 状态 |
|----|----|------|------|
| #492 | 底座 | LLMDecisionRecord + 20260824_001 + risk_tier_for(R0/R1/R2/blocked) | ✅ 已合 develop |
| #493 | 运维 | invocation_scope 上下文（run/version/entity/env）+ 链总墙钟 + P95/P99 + 完整性审计 | ✅ 已合 |
| #494 | JD | 评测三口径（LLM-only/raw/aligned）+ commit/gold SHA/provider 证据信封 + jd_raw.content_hash 重抽（重爬不重抽治理，20260824_002） | ✅ 已合 |
| #495 | 归一 | 岗位名/技能名 LLM 决策器 + 硬门（防自创名/虚构标准名/同义反复） | ✅ 已合 |
| #496 | 归一 | name_normalization_shadow_daily（ETL 阶段 19，shadow 只落决策记录） | ✅ 已合 |
| #497 | 分类 | 技能分类接决策信封（skill_classify shadow）+ 岗位分类决策器（清单内强制选择） | ✅ 已合 |
| #498 | 簇 | 岗位职能域 LLM 命名接决策记录（cluster_label shadow） | ✅ 已合 |
| #499 | 治理 | dict-guard 统一风险路由 auto→R1/auto_applied、proposal→R2/proposal | ✅ 已合 |
| #500 | 关系 | 技能关系决策器（类型/方向 + 硬门 + 先修环纯函数），tier 一律 R2 | ✅ 已合 |
| #501 | 关系 | propose_skill_relations 脚本：JD 共现候选 → LLM 判定 → proposal 决策记录 | ⏳ CI 重跑中 |
| #502 | 管理 | GET /admin/llm-decisions（分页过滤）+ /summary（domain×status）+ 契约先行 | ✅ 已合 |
| #503 | 管理 | 「LLM 决策与验收」页：汇总卡 + 过滤列表（admin RBAC） | ✅ 已合 |

## 三、验收矩阵（代码层可核验）

| 门槛 | 目标 | 当前状态 |
|------|------|---------|
| JD aligned F1（110 条三次真实 LLM 中位） | ≥0.90 | **0.9629**（正式三跑达标，极差 0.0004，report 见 docs/reviews/验收评测_20260824.md） |
| JD LLM-only/raw F1 | ≥0.87 | 三跑产物已含三口径（#494），正式值随 08-25 复跑归档 |
| 名称归一自动合并数 | 首窗口 0 | shadow 阶段，0 |
| dict-guard 自动生效 | R1 档带置信/影响面门 | unchanged（tier_for 未改，仅新增落档） |
| 技能关系自动写边 | 首窗口 0 | proposal 全部 R2，环/门失败 blocked |
| 决策记录完整性 | purpose/model/run_id 100% | #493 完整性审计字段就绪 |
| 测试 | 后端全量 + 前端 typecheck/lint | 后端 1988 passed；前端 typecheck 通过、lint 零新增 |

## 四、226 灰度部署清单（阴影功能默认关闭，按窗口开启）

1. **镜像重建**：develop 最新（含 #492~#503）rebuild api/worker + `alembic upgrade head`（迁移 20260824_001/002 会建决策表、加 jd_raw.content_hash）。
2. **先开 shadow 观察窗口**（默认均 false，按管理后台「运行时配置」或在 226 上直接改配置后重启 worker）：
   - `name_normalization_shadow_enabled=true`（岗位/技能名归一 shadow）
   - `skill_category_review_enabled=true`（技能分类提议，沿用既有 suggested_* 通道）
   - 关系提议走脚本 `uv run python scripts/propose_skill_relations.py --limit 40`（手动/cron，不进 ETL 自动链）
3. **观察 3 个 ETL 日窗口**：管理员在 `/admin/llm-decisions` 查看六域 shadow/proposal/blocked 量与样本；对照分类/归一黄金集阈值（分类 top-1 ≥90%、skill 归一合并 precision ≥95%、岗位归一 accuracy ≥90%——黄金集冻结见下文遗留项）。
4. **达标后逐域放行**：R2（解禁/归并/删除/关系）保持人工审批；R0/R1 最多 10% 自动灰度，连续 3 窗口满足质量/成本/延迟/误杀门槛后再扩。

## 五、遗留与负责人

| 项 | 说明 | 负责人 |
|----|------|--------|
| 分类/归一/关系黄金集冻结 | 150 分类 + 150 归一 pair + 100 关系正反例（见执行计划口径），需真实 LLM 三跑取中位 | 张恺天 + 测试 |
| 关系审核执行通道 | proposal→approved 后写动态关系表、图同步与 YAML 种子合并 | 后端 + 张恺天 |
| 决策记录接入 ETL 冲突候选 | 名称归一冲突/分类冲突作为治理候选源（PR5 预留方向） | 后端 |
| spark/qwen fallback | 密钥 401 潜伏容灾缺口（08-22 实证） | 运维 |
| 拼音/缩写切片指标 | skill 归一短词保护切片错误合并=0 的持久回归 | 测试 |

## 六、结论

**第一阶段「六域 LLM 语义主导」已全部落地并合入 develop**：所有结构化决策可追踪、可回放、可比较（决策信封 + 版本字段 + 输入哈希 + 审计），生产边界（不变量门禁 + 人工审批 + 自动写图=0）保持。剩余为黄金集冻结、关系审批执行与 226 观察窗口，属运行/算法侧跟进，代码侧闭环。

> 归档说明：本报告随 docs/ 分支入库；三跑原始 JSON 在 226 容器 `/tmp/eval/`（08-24 正式归档规范要求逐步长存档，修订见 #494 证据信封）。
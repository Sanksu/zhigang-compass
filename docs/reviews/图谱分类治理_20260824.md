# 图谱分类治理报告 — 2026-08-24

> 执行：ox-alpha（用户 Sanksu 授权「处理以上问题并验证」）；线上实例 192.168.0.226。
> 上游输入：同日《图谱分类评价》（盘点发现 P0 技能分类覆盖 12.8% / P1 域名错位与孤立岗 / P2 三套词表不对齐与死属性）。

## 一、处置总览（六 PR）

| PR | 内容 | 状态 |
|----|------|------|
| #469 | 未分类技能批量 LLM 分类提议脚本（req_count 降序批通道） | ✅ 已合 develop |
| #470 | 岗位域语义命名（--llm-name）+ 孤立岗兜底归通用域 | ⚠️ **待张恺天审**（含命名 prompt，算法红线留痕；CI 绿） |
| #471 | 图谱视图移除死属性 communityId | ✅ 已合 develop（**不完整**：漏 add graph.py/queries.py，#475 补全） |
| #472 | Tool 节点分类回退白名单词表 + 存量回填脚本 | ✅ 已合 develop |
| #473 | 白名单扩容 605→1093 条、类目 23→20 归并 | ✅ 已合 develop（负责人四项拍板留痕 + zkt 知会；已部署 226 + 存量回填 491 节点） |
| #474 | dict-guard 门禁测试去抽样化（修哈希序 flaky） | ✅ 已合 develop |
| #475 | communityId 清理补全（#471 漏入库的 graph.py/queries.py） | ✅ 已合 develop |

## 二、P0 技能分类补全（#469 + 线上实跑 + #473 草案）

1. **批量提议实跑**：`scripts/batch_skill_category_propose.py` 在 zhigang-api 容器内执行，req_count 前 600 未分类技能（deepseek、并发 6、单条 15s 超时）→ **600/600 提议成功，0 失败**；高置信（≥0.70）494 条写入 `suggested_category*` 提议字段（权威 category 不动）。报告：容器 `/app/reports/skill_category_batch_2026-08-24.json`（已取回 temp/ 入档副本）。
2. **白名单扩容草案（#473，未合）**：+488 条高置信条目（去重后），605→1093；类目归并「工程→工程协作 / 算法→AI/机器学习 / 数据处理→大数据」+ 旧三条目迁移 → **23 类收敛为 20 类**。排除：软技能提议 ×1（English，影响匹配展示通道）、岗位族冲突 ×3、大小写变体 ×2 对。
3. **晋升效果预估（合并部署后）**：白名单外未分类将从 3835 降至 ~3345；用户可见视图（techStack 前 600）分类覆盖 52%→~95%；高频核心概念（MLOps/.NET/JVM/GPU/CUDA/RBAC/OAuth 等）全部入表。

### 扩容实证的两个语义陷阱（审查重点）
- **岗位族词表冲突**：「FPGA验证」被 LLM 提名为技能并入草案后，`normalize_position_name("FPGA团队")` 的碎片重定向被第 6 步「技能词不入图」守卫打穿返回空串（`test_cn_fragments` 实证失败）。已加全量冲突过滤（_POSITION_WHITELIST/别名/重定向目标/tech-stack 展示名），剔除 FPGA验证/AI智能体/AI自动化。
- **大小写唯一约束**：Cloudbees/DataDog 与现行 CloudBees/Datadog 撞 `normalize_skill` 小写映射，保留官方拼写。
- **测试夹具随行更新**：`test_skill_coverage_unmapped_english` 原样本 Redshift/Kinesis 已合法入表（命中=正确行为），换域外长尾词 + 前置断言（未来撞样本显式报错提示换词）。

## 三、P1 岗位域（#470 待审 + 已在线上验证）

线上重跑（含 --llm-name）：**公开状态岗位域覆盖 96/138 → 123/123 = 100%**（15 个无域岗均为 candidate 待审态，正确排除）。域名从代表岗名升级为语义职能域名：

| 前（代表岗名） | 后（LLM 语义名） | 岗数 |
|---|---|---|
| 数据分析师 | 数据分析 | 20 |
| 项目经理 | 项目管理 | 15 |
| 机器视觉算法工程师 | 智能算法 | 6 |
| CT技师 | 医学影像技术 | 2 |
| AIGEO优化 | AI搜索优化 | 2 |
| 创始工程师 | （归通用域/各语义簇） | — |

共 15 域（14 语义 + 通用桶）。机制补强：① 孤立岗（无合格投影边不进图）写回后统一兜底 `dom_general`；② `--llm-name` 单次 LLM 调用批量命名，Pydantic 强校验 + sanitize 纯函数（未知簇键/跨簇重名/与成员岗同名回退代表岗名），失败整体回退不阻塞划分。

## 四、P2 卫生项（#471/#472/#474 + 线上运维）

- **community_id 死属性清零**：API 出参移除（前端/openapi/docs 零消费实证）；Neo4j REMOVE 清理 411 个节点残留属性。⚠️ 执行教训：#471 提交时仅 add queries_async.py，graph.py/queries.py 改动遗留工作区未入库（CI 无断言未拦截），#475 补全——commit 前核对 `git status` 与改动清单一致的老纪律再次验证。
- **Tool 空类目清零**：入图侧 `effective_tool_category` 回退白名单词表；存量回填 2346 个（词表命中 229 真实类目 + 哨兵 2117），空串不复存在。
- **flaky 测试修复（#474）**：`test_add_stopword_vetoes_whitelist` 用 `next(iter(SKILL_WHITELIST))` 随机抽样，抽中双重身份词（多线程等五个待裁决核心词同时在白名单与停用词表）或单字符词（C）时拒绝理由不同而断言翻车——扩容改变集合内容后暴露。改全量遍历 + 三类拒绝理由兼容，不变量不变（白名单词绝不可入停用词）。

## 五、验证口径（前后对比）

| 指标 | 前（08-24 盘点） | 后（本轮收尾） |
|------|------|------|
| Skill 有真实分类（权威 category） | 564/4399 = 12.8% | **1052/4407 = 23.9%**（#473 拍板部署后存量回填 491 节点；长尾每日审查通道续扫） |
| Skill 高置信分类提议 | 0 条 | 600 条（≥0.70 共 494） |
| techStack 视图前 600 分类覆盖 | 52%（289 未分类） | **91.7%（未分类 50）**（部署回填后实测） |
| 公开岗位域覆盖 | 96/138 = 69.6% | **123/123 = 100%** |
| 域名语义 | 代表岗名（CT技师级噪音） | 15 个语义职能域 |
| Tool 空 category | 2670 | **0** |
| community_id 残留属性 | 411 节点 | **0** |
| 测试回归 | — | extraction/kg/graph/workers/data_quality 全绿（含 733 例批次） |

## 六、遗留与风险

1. **#470 仍待 zkt 审**（含 LLM 命名 prompt，CI 绿；未合前勿用 develop 旧脚本重跑 domain sync，会把语义域名打回代表岗名）。#473 已由负责人四项拍板放行（全量批准/归并批准/剔除确认/合并即部署，AskUserQuestion 留痕 + zkt 知会）并完成部署：GHCR 镜像滚动更新 + backfill_skill_category 回填 491 节点、容器内词表 1093/20 类实证。#470 未合前**不要用 develop 旧脚本重跑 domain sync**（会把语义域名打回代表岗名）。
2. **五个停用词解禁仍 pending**（多线程/数据库/数据结构/消息队列/缓存）：与英文等价物存活形成跨语言不对称（Data Structures 74° 存活 vs 数据结构停用）——本轮扩容后英文侧多数已入白名单受保护，不对称进一步凸显，建议优先裁决。
3. **长尾未分类仍有 ~3300**（度数 1-4 为主）：每日 ETL 审查通道继续扫尾；如需再批量可复跑 batch 脚本 --refresh。
4. **dict-guard 审批端点原子性整改**在途（工作区有未提交改动，非本会话产出）：先校验→落库→后副作用，勿误删。

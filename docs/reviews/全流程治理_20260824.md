# 全流程闭环治理报告 — 2026-08-24 凌晨

> 执行：ox-alpha；线上实例 192.168.0.226（tangyao 机器），develop 镜像（api/worker 08-23 19:57 CST 重建）。
> 范围：采集 → 入库 → ETL → 图谱 → 匹配/学习路径 → 演化 → API/前端 → 运维面。
> 同日图谱治理轮次详见《图谱治理_20260824.md》。

## 一、总览：九环节健康度

| # | 环节 | 状态 | 关键证据 |
|---|------|------|---------|
| 1 | 基础层 | ✅ 健康 | 5 容器 0 重启全 healthy；内存余量足；磁盘 80%（余 179G） |
| 2 | 采集-JD | ✅ 三主源活 | linkedin_public 73条/2d（每日08:00）、indeed 120条/2d、zhilian 33条/2d（04:01） |
| 3 | 采集-课程 | ⚠️ 瞬时故障已恢复 | icourse163 08-22/23 DNS 解析失败（CannotResolveHostError），当前五域名解析全通；edx/coursera 有增量 |
| 4 | 入库/指纹 | ✅ | jd_raw 9748 条七源在库；embedding 三表随 ETL 全新鲜 |
| 5 | ETL 管线 | ✅ 配置正确，待今日首火 | cron 05:00 已注册、幂等锁日期键不阻塞；昨日 18:18 CST ETL 链实证跑通（提案59条+演化事件+快照v19）；worker 昨晚重建后 reports 空属预期 |
| 6 | 图谱 | ✅ 健康 | Skill 4399（归一化覆盖 100%）、REQUIRES 11428、SIMILAR_TO 711、孤儿仅 2；dict-guard 积压清零 |
| 7 | 匹配/演化 | ✅ | match_recommend 近期 39 成功；discovery 三态 stable25/candidate4/emerging3；evolution_events 7天17条 |
| 8 | API/前端 | ✅ | /health 200@1ms；graph 四视图全 200 亚秒（panorama 598KB@1.0s）；全文搜索 7ms；SPA fallback 200 |
| 9 | 运维面 | ⚠️ 两处整改 | 见下「本轮修复」；ALERT_WEBHOOK_URL 未配 |

## 二、本轮已执行的修复

1. **宿主 crontab 死项清理**：`/tmp/rtk_force_restart.sh`（文件已不存在却每分钟被调用）已从 crontab 移除；备份存 `~/crontab.backup.20260824_015238`。仅保留 v2ray @reboot 行。
2. **旧镜像标签回收**：`zhigang-compass-api:latest` / `zhigang-compass-worker:latest`（各 8.46GB）确认无容器引用后删除；与 ghcr develop 镜像共享层，实际磁盘释放有限，主要消除版本混淆。
3. （前轮）79 条 dict-guard 提案裁决执行完毕。

## 三、待观察 / 待拍板

**待观察（无需决策）**
- 今日 05:00 CST ETL 首火：复查点 = worker 日志 run_etl_pipeline_scheduled 成功、reports/ 目录落盘（etl/dict_guard/graph_health 报告）、glassdoor 是否产出（已连续 3 天 0 新增，若再 0 需查 spider 反爬/代理路由）。
- icourse163 DNS：瞬时故障已自愈，若复现考虑 worker 加 DNS fallback 或重试窗口。

**待拍板（一行一项）**
1. remove_stopword×5（多线程/数据库/数据结构/消息队列/缓存 解禁）——算法语义变更，等 zkt。
2. 孤岛课 528 个治理方向：全部带 source_url 且持续新增 → 建议查 ETL 建边阶段阈值覆盖面（补边优于删课）。
3. ALERT_WEBHOOK_URL 配置：一行环境变量即可激活 send_alert 告警链路，需提供 webhook 地址。
4. dict-guard 每日候选上限 20/日是否维持（本轮积压已清零，暂无压力）。

## 四、建议代码 PR（按优先级）

1. **fix(be): dict_guard 审批端点原子性**——`review_proposal` 当前顺序为 动态层变更 → Neo4j 删除 → PG 提交；中途失败（如 AuditLog UUID 校验）会造成图谱已删而 PG 回滚的半执行态（本轮首跑全灭实证）。修法：operator 先做 UUID 格式校验前置 + 图谱删除移到 commit 之后。
2. **feat(data): 孤岛课建边覆盖率诊断脚本**——输出未建边课程的技能命中分布，定位是阈值过严还是阶段跳过。
3. **fix(be): 技能别名归一**——Full Stack Development 类真实变体应映射到「全栈」而非停用（驳回提案的落地通道）。

## 五、体检明细附录

- JD 七源存量：linkedin_public 3230 / indeed 2692 / zhilian 2520 / glassdoor 1134 / boss 112(暂停·设计内) / monster 50 / maimai 10。
- 课程三源：icourse163 894 / coursera 370 / edx 229。
- embedding：jd 10052 / skill 19786 / project 4，最新更新均 08-23。
- Redis：db0 缓存 57 keys、db1 ARQ 3 keys，正常水位。
- LLM provider：deepseek 健康检查通过（每 5 分钟 cron）；spark/qwen 此前已 enabled:false。
- task_status 失败样本：zhilian 单次 0 产出（同日另次成功 15 条）、icourse163/edx/stackoverflow 08-22 退出码 1（瞬时网络），均有重试或后续成功兜底。

> 归档说明：本报告 2026-08-24 自工作目录整理入档；执行明细（逐条提案结果 JSON）存 temp/ 不入库。

---

## 勘误（2026-08-24 上午复查追加）

§一「ETL 管线 ✅ 配置正确，待今日首火」结论**作废**：05:35 定时复查实证 ETL 从未在 05:00 CST 点火——worker 容器 TZ=UTC，arq 0.28 cron 按进程本地时间触发（无 timezone 参数），`etl_run_hour=5` 实为北京 13:00。已修复：PR #478（TZ=Asia/Shanghai）+ 226 应用重建 + 当日 09:08 手动补触发（14 分钟全链跑通、报告落盘、indeed +58）。另：§三「icourse163 DNS 瞬时故障已自愈」维持成立；glassdoor 于当日午后查明根因（CDP 浏览器代理 + 人工 Cloudflare 验证两前置缺位）并按 boss 先例配置停用，详见 CHANGELOG 08-24。

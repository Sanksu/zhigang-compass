# 图谱治理轮次报告 — 2026-08-24

> 执行：ox-alpha（用户 Sanksu 授权批量治理）；线上实例 192.168.0.226，develop 镜像，alembic 20260823_001 与 develop HEAD 同步。

## 一、背景与盘点（执行前）

| 指标 | 值 |
|------|-----|
| Skill 节点 | 4441（孤儿 2，度=1 有 1387 由 graph_health_daily 自动轨道清理） |
| Position 节点 | 139（无明显脏节点残留） |
| Course 节点 | 1491，其中 546 完全无边（36.6%） |
| dict-guard 待审提案 | **79 条积压**（08-21 起，置信度 0.45~0.90 全部低于自动档 0.8） |
| 已自动执行变更 | 40 条留痕 |

## 二、79 条提案裁决明细

**批准 69 条**（理由模板见 `temp/dg_review.py`（工作脚本不入库），全量逐条结果见 `dg_review_result_20260824.json`）：
- 技能加停用词 ×43：IBM WebSphere 产品族、医学词、水文环保、纺织服装、公共经济学教材词等非技术域长尾噪音。动态 blocked + 同名 Skill 节点清理。
- 课程删节点 ×19：孤岛课程（专升本培训班/C4D 合辑/思维导图课/AI For Everyone 重复孤本/Google Data Analytics 死记录等）。
- 课程删边 ×6：`*→Everyday Excel` LEARNABLE_VIA 相似度 0.209 < 0.30 同语言门控阈值。
- 岗位删节点 ×1：`AI与智能体` 孤岛伪岗位（jd_snapshots=0）。

**驳回 5 条**（状态 rejected，理由已写入提案）：
| 词 | 驳回理由 |
|----|---------|
| Full Stack Development | 真实全栈技能变体；图内已有「全栈」(176度)/Full-Stack Web Development(20度)，应走归一映射而非停用 |
| BPEL | WS-BPEL 真实遗留技术技能，低频但语义有效 |
| 数据策略 | 近7日 JD 语料命中4次，疑似真实需求词 |
| 智能建筑 | 楼宇智能化岗位域真实概念 |
| 网络布线 | IT 基础设施职业技能 |

**跳过 5 条（保持 pending，算法红线待张恺天）**：
`多线程 / 数据库 / 数据结构 / 消息队列 / 缓存` 移除停用词提案——五个核心 CS 技能疑被早期过度去重误停，解禁会建真节点改变匹配语义，需 zkt 拍板（批准路径=端点自动 protect 受影响技能穿透停用词）。

## 三、执行与验证

- 执行方式：api 容器内以应用自身 JWT 函数签发临时 admin 令牌（sub=users.id UUID），走真实 HTTP 审批端点 `/api/v1/admin/dict-guard/proposals/{id}/review`，DictChangeLog(source=manual) + AuditLog 全留痕。
- 第 2 轮执行 74/74 成功（69 approve + 5 reject），0 失败。
- 验证：proposals approved=69/rejected=5/pending=5 ✓；manual 变更 blocked43/edge6/node20 ✓；43 个停用词全部不在图中 ✓；动态过滤层 blocked=50 无重复（add_entry 幂等）✓。
- 图谱变化：Skill 4441→4399（-42，其中一名下两案或一节点先失）；Course 1491→1472；Position 139→138；LEARNABLE_VIA 7210→7175（-6 显式删边 -29 技能 DETACH 连带）；孤岛课 546→528。

## 四、事故与教训（本轮踩坑）

1. **首跑全灭根因**：临时令牌 `sub="admin"`（用户名），而 AuditLog.user_id 是 UUID 列 → asyncpg invalid UUID → 全部 500。修正为 users.id UUID 后成功。**教训：容器内签发运维令牌 sub 必须用 users.id。**
2. **⚠️ 端点原子性隐患（建议提 PR）**：`review_proposal` 的执行顺序是 动态层变更 → **Neo4j 删除** → PG 提交（含 AuditLog）。首跑在最后一步失败时，图谱删除已生效且不可回滚、PG 回滚不知情——第二轮"追认"才对齐。图谱删除应移到 PG 提交之后，或先做请求级校验（如 operator UUID 格式）。此隐患对 remove_node/remove_edge 类提案普遍存在，非本次特有。
3. 首跑的删除效果因此全部记 removed_units=0（实际删除发生于首跑）。本文件第三节验证数字为准。

## 五、遗留与后续

1. **pending×5 移除停用词**：待 zkt 拍板后在管理面板审批（或再次授权批量执行）。
2. **孤岛课程 528 成因诊断**（本轮只读结论）：全部带 source_url，采集时间横跨 08-13~08-23（最新至前一日 18:58）→ 不是死数据，是**采集入库但 ETL 未建 LEARNABLE_VIA 边**的活课程。分布 icourse163 439 / edx 66 / coursera 23。方向：排查建边阶段的匹配阈值与覆盖面，而非删课。
3. 驳回项跟进：Full Stack Development 应配置到「全栈」的别名/归一映射（position_duplicate_cleanup 的 alias 思路同理可用于技能侧）。
4. 长尾调优仍待决策：dict_guard_max_candidates=20/日 vs 积压增速；本轮清完后积压归零（剩 5 条有意保留）。

> 归档说明：本报告 2026-08-24 自工作目录整理入档；执行明细（逐条提案结果 JSON）存 temp/ 不入库。

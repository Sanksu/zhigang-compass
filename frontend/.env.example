# 智岗罗盘前端环境变量
# 复制为 .env.local 后按需修改；.env.local 已被 .gitignore 忽略

# 开发态后端 API 地址（vite.config.ts 中作为 proxy target）
# 默认 http://localhost:8000；生产无独立反代——FastAPI 同端口托管 frontend/dist
# （compose 将 dist 只读挂载进 api 容器），见仓库根 DEPLOY.md
VITE_API_TARGET=http://localhost:8000
